using Spice86.Core.CLI;
using Spice86.Core.Emulator.CPU.Exceptions;
using Spice86.Core.Emulator.Function;
using Spice86.Core.Emulator.ReverseEngineer;
using Spice86.Core.Emulator.VM;
using Microsoft.Extensions.Logging;
using Spice86.Shared.Emulator.Memory;
using System;
using System.Collections.Generic;

namespace Spice86.Generated;

public sealed class CfgGeneratedOverrideSupplier : IOverrideSupplier {
    public IDictionary<SegmentedAddress, FunctionInformation> GenerateFunctionInformations(ILogger loggerService, Configuration configuration, ushort programStartAddress, Machine machine) {
        return new CfgGeneratedOverrides(new Dictionary<SegmentedAddress, FunctionInformation>(), machine, loggerService, configuration, programStartAddress).FunctionInformations;
    }
}

public class CfgGeneratedOverrides : CSharpOverrideHelper {
    protected readonly ushort cs1;

    public CfgGeneratedOverrides(IDictionary<SegmentedAddress, FunctionInformation> functionInformations, Machine machine, ILogger loggerService, Configuration configuration, ushort programStartSegment) : base(functionInformations, machine, loggerService, configuration) {
        cs1 = 0xF000;

        DefineFunction(cs1, 0xFFF0, unknown_F000_FFF0_FFFF0);
    }

    public virtual Action unknown_F000_FFF0_FFFF0(int loadOffset) {
        goto label_F000_FFF0_FFFF0_0;

    label_F000_0000_F0000_1:
        CheckExternalEvents(cs1, 0x0000);
        // F000:0000 mov SP,0x00C0
        SP = (ushort)0x00C0;
        // F000:0003 mov BX,3
        BX = (ushort)0x0003;
        // F000:0006 mov AX,7
        AX = (ushort)0x0007;
        // F000:0009 mov DX,0xFFFF
        DX = (ushort)0xFFFF;
        // F000:000C mul BX
        uint result_F000_000C_F000C = Alu16.Mul(AX, unchecked((ushort)unchecked((short)BX)));
        DX = unchecked((ushort)(result_F000_000C_F000C >> 16));
        AX = unchecked((ushort)result_F000_000C_F000C);
        // F000:000E mov word ptr DS:[0],AX
        UInt16[DS, (ushort)0x0000] = AX;
        // F000:0011 mov word ptr DS:[2],DX
        UInt16[DS, (ushort)0x0002] = DX;
        // F000:0015 pushf
        Stack.Push16(FlagRegister16);
        // F000:0016 mov DX,0xA320
        DX = (ushort)0xA320;
        // F000:0019 mov AX,0xFFFF
        AX = (ushort)0xFFFF;
        // F000:001C mul DX
        uint result_F000_001C_F001C = Alu16.Mul(AX, unchecked((ushort)unchecked((short)DX)));
        DX = unchecked((ushort)(result_F000_001C_F001C >> 16));
        AX = unchecked((ushort)result_F000_001C_F001C);
        // F000:001E mov word ptr DS:[4],AX
        UInt16[DS, (ushort)0x0004] = AX;
        // F000:0021 mov word ptr DS:[6],DX
        UInt16[DS, (ushort)0x0006] = DX;
        // F000:0025 pushf
        Stack.Push16(FlagRegister16);
        // F000:0026 mov AX,0xFFFF
        AX = (ushort)0xFFFF;
        // F000:0029 mov word ptr DS:[8],1
        UInt16[DS, (ushort)0x0008] = (ushort)0x0001;
        // F000:002F mul word ptr DS:[8]
        uint result_F000_002F_F002F = Alu16.Mul(AX, unchecked((ushort)unchecked((short)UInt16[DS, (ushort)0x0008])));
        DX = unchecked((ushort)(result_F000_002F_F002F >> 16));
        AX = unchecked((ushort)result_F000_002F_F002F);
        // F000:0033 mov word ptr DS:[0x000A],AX
        UInt16[DS, (ushort)0x000A] = AX;
        // F000:0036 mov word ptr DS:[0x000C],DX
        UInt16[DS, (ushort)0x000C] = DX;
        // F000:003A pushf
        Stack.Push16(FlagRegister16);
        // F000:003B mov AX,0xFFFF
        AX = (ushort)0xFFFF;
        // F000:003E mov word ptr DS:[0x000E],0xFFFF
        UInt16[DS, (ushort)0x000E] = (ushort)0xFFFF;
        // F000:0044 mul word ptr DS:[0x000E]
        uint result_F000_0044_F0044 = Alu16.Mul(AX, unchecked((ushort)unchecked((short)UInt16[DS, (ushort)0x000E])));
        DX = unchecked((ushort)(result_F000_0044_F0044 >> 16));
        AX = unchecked((ushort)result_F000_0044_F0044);
        // F000:0048 mov word ptr DS:[0x0010],AX
        UInt16[DS, (ushort)0x0010] = AX;
        // F000:004B mov word ptr DS:[0x0012],DX
        UInt16[DS, (ushort)0x0012] = DX;
        // F000:004F pushf
        Stack.Push16(FlagRegister16);
        // F000:0050 mov AX,0x46DB
        AX = (ushort)0x46DB;
        // F000:0053 mov BP,0
        BP = (ushort)0x0000;
        // F000:0056 mul BP
        uint result_F000_0056_F0056 = Alu16.Mul(AX, unchecked((ushort)unchecked((short)BP)));
        DX = unchecked((ushort)(result_F000_0056_F0056 >> 16));
        AX = unchecked((ushort)result_F000_0056_F0056);
        // F000:0058 mov word ptr DS:[0x0014],BP
        UInt16[DS, (ushort)0x0014] = BP;
        // F000:005C mov word ptr DS:[0x0016],AX
        UInt16[DS, (ushort)0x0016] = AX;
        // F000:005F mov word ptr DS:[0x0018],DX
        UInt16[DS, (ushort)0x0018] = DX;
        // F000:0063 pushf
        Stack.Push16(FlagRegister16);
        // F000:0064 mov AX,0x46DB
        AX = (ushort)0x46DB;
        // F000:0067 mov SI,0xEEEB
        SI = (ushort)0xEEEB;
        // F000:006A mul SI
        uint result_F000_006A_F006A = Alu16.Mul(AX, unchecked((ushort)unchecked((short)SI)));
        DX = unchecked((ushort)(result_F000_006A_F006A >> 16));
        AX = unchecked((ushort)result_F000_006A_F006A);
        // F000:006C mov word ptr DS:[0x001A],SI
        UInt16[DS, (ushort)0x001A] = SI;
        // F000:0070 mov word ptr DS:[0x001C],AX
        UInt16[DS, (ushort)0x001C] = AX;
        // F000:0073 mov word ptr DS:[0x001E],DX
        UInt16[DS, (ushort)0x001E] = DX;
        // F000:0077 pushf
        Stack.Push16(FlagRegister16);
        // F000:0078 mov BL,0x14
        BL = (byte)0x14;
        // F000:007A mov AX,0xFF07
        AX = (ushort)0xFF07;
        // F000:007D mov DX,0xFFFF
        DX = (ushort)0xFFFF;
        // F000:0080 mul BL
        ushort result_F000_0080_F0080 = Alu8.Mul(AL, unchecked((byte)unchecked((sbyte)BL)));
        AH = unchecked((byte)(result_F000_0080_F0080 >> 8));
        AL = unchecked((byte)result_F000_0080_F0080);
        // F000:0082 mov word ptr DS:[0x0020],AX
        UInt16[DS, (ushort)0x0020] = AX;
        // F000:0085 mov word ptr DS:[0x0022],DX
        UInt16[DS, (ushort)0x0022] = DX;
        // F000:0089 pushf
        Stack.Push16(FlagRegister16);
        // F000:008A mov CH,0x24
        CH = (byte)0x24;
        // F000:008C mov AX,0x00FF
        AX = (ushort)0x00FF;
        // F000:008F mul CH
        ushort result_F000_008F_F008F = Alu8.Mul(AL, unchecked((byte)unchecked((sbyte)CH)));
        AH = unchecked((byte)(result_F000_008F_F008F >> 8));
        AL = unchecked((byte)result_F000_008F_F008F);
        // F000:0091 mov word ptr DS:[0x0024],AX
        UInt16[DS, (ushort)0x0024] = AX;
        // F000:0094 mov word ptr DS:[0x0026],DX
        UInt16[DS, (ushort)0x0026] = DX;
        // F000:0098 pushf
        Stack.Push16(FlagRegister16);
        // F000:0099 mov AX,0x00FF
        AX = (ushort)0x00FF;
        // F000:009C mov byte ptr DS:[0x0028],1
        UInt8[DS, (ushort)0x0028] = (byte)0x01;
        // F000:00A1 mul byte ptr DS:[0x0028]
        ushort result_F000_00A1_F00A1 = Alu8.Mul(AL, unchecked((byte)unchecked((sbyte)UInt8[DS, (ushort)0x0028])));
        AH = unchecked((byte)(result_F000_00A1_F00A1 >> 8));
        AL = unchecked((byte)result_F000_00A1_F00A1);
        // F000:00A5 mov word ptr DS:[0x0029],AX
        UInt16[DS, (ushort)0x0029] = AX;
        // F000:00A8 mov word ptr DS:[0x002B],DX
        UInt16[DS, (ushort)0x002B] = DX;
        // F000:00AC pushf
        Stack.Push16(FlagRegister16);
        // F000:00AD mov AX,0xFFFF
        AX = (ushort)0xFFFF;
        // F000:00B0 mov byte ptr DS:[0x002D],0xFF
        UInt8[DS, (ushort)0x002D] = (byte)0xFF;
        // F000:00B5 mul byte ptr DS:[0x002D]
        ushort result_F000_00B5_F00B5 = Alu8.Mul(AL, unchecked((byte)unchecked((sbyte)UInt8[DS, (ushort)0x002D])));
        AH = unchecked((byte)(result_F000_00B5_F00B5 >> 8));
        AL = unchecked((byte)result_F000_00B5_F00B5);
        // F000:00B9 mov word ptr DS:[0x002E],AX
        UInt16[DS, (ushort)0x002E] = AX;
        // F000:00BC mov word ptr DS:[0x002E],DX
        UInt16[DS, (ushort)0x002E] = DX;
        // F000:00C0 pushf
        Stack.Push16(FlagRegister16);
        // F000:00C1 mov AX,0x00C5
        AX = (ushort)0x00C5;
        // F000:00C4 mov DX,0
        DX = (ushort)0x0000;
        // F000:00C7 mul DL
        ushort result_F000_00C7_F00C7 = Alu8.Mul(AL, unchecked((byte)unchecked((sbyte)DL)));
        AH = unchecked((byte)(result_F000_00C7_F00C7 >> 8));
        AL = unchecked((byte)result_F000_00C7_F00C7);
        // F000:00C9 mov word ptr DS:[0x0030],DX
        UInt16[DS, (ushort)0x0030] = DX;
        // F000:00CD mov word ptr DS:[0x0032],AX
        UInt16[DS, (ushort)0x0032] = AX;
        // F000:00D0 pushf
        Stack.Push16(FlagRegister16);
        // F000:00D1 mov AL,0xB5
        AL = (byte)0xB5;
        // F000:00D3 mov DH,0xF9
        DH = (byte)0xF9;
        // F000:00D5 mul DH
        ushort result_F000_00D5_F00D5 = Alu8.Mul(AL, unchecked((byte)unchecked((sbyte)DH)));
        AH = unchecked((byte)(result_F000_00D5_F00D5 >> 8));
        AL = unchecked((byte)result_F000_00D5_F00D5);
        // F000:00D7 mov word ptr DS:[0x0034],SI
        UInt16[DS, (ushort)0x0034] = SI;
        // F000:00DB mov word ptr DS:[0x0036],AX
        UInt16[DS, (ushort)0x0036] = AX;
        // F000:00DE mov word ptr DS:[0x0038],DX
        UInt16[DS, (ushort)0x0038] = DX;
        // F000:00E2 pushf
        Stack.Push16(FlagRegister16);
        // F000:00E3 mov BX,3
        BX = (ushort)0x0003;
        // F000:00E6 mov AX,7
        AX = (ushort)0x0007;
        // F000:00E9 mov DX,0xFFFF
        DX = (ushort)0xFFFF;
        // F000:00EC imul BX
        int result_F000_00EC_F00EC = Alu16.Imul(unchecked((short)AX), unchecked((short)BX));
        DX = unchecked((ushort)(result_F000_00EC_F00EC >> 16));
        AX = unchecked((ushort)result_F000_00EC_F00EC);
        // F000:00EE mov word ptr DS:[0x003C],AX
        UInt16[DS, (ushort)0x003C] = AX;
        // F000:00F1 mov word ptr DS:[0x003E],DX
        UInt16[DS, (ushort)0x003E] = DX;
        // F000:00F5 pushf
        Stack.Push16(FlagRegister16);
        // F000:00F6 mov DX,0xA320
        DX = (ushort)0xA320;
        // F000:00F9 mov AX,0xFFFF
        AX = (ushort)0xFFFF;
        // F000:00FC imul DX
        int result_F000_00FC_F00FC = Alu16.Imul(unchecked((short)AX), unchecked((short)DX));
        DX = unchecked((ushort)(result_F000_00FC_F00FC >> 16));
        AX = unchecked((ushort)result_F000_00FC_F00FC);
        // F000:00FE mov word ptr DS:[0x0040],AX
        UInt16[DS, (ushort)0x0040] = AX;
        // F000:0101 mov word ptr DS:[0x0042],DX
        UInt16[DS, (ushort)0x0042] = DX;
        // F000:0105 pushf
        Stack.Push16(FlagRegister16);
        // F000:0106 mov AX,0xFFFF
        AX = (ushort)0xFFFF;
        // F000:0109 mov word ptr DS:[0x0044],1
        UInt16[DS, (ushort)0x0044] = (ushort)0x0001;
        // F000:010F imul word ptr DS:[0x0044]
        int result_F000_010F_F010F = Alu16.Imul(unchecked((short)AX), unchecked((short)UInt16[DS, (ushort)0x0044]));
        DX = unchecked((ushort)(result_F000_010F_F010F >> 16));
        AX = unchecked((ushort)result_F000_010F_F010F);
        // F000:0113 mov word ptr DS:[0x0046],AX
        UInt16[DS, (ushort)0x0046] = AX;
        // F000:0116 mov word ptr DS:[0x0048],DX
        UInt16[DS, (ushort)0x0048] = DX;
        // F000:011A pushf
        Stack.Push16(FlagRegister16);
        // F000:011B mov AX,0xFFFF
        AX = (ushort)0xFFFF;
        // F000:011E mov word ptr DS:[0x004A],0xFFFF
        UInt16[DS, (ushort)0x004A] = (ushort)0xFFFF;
        // F000:0124 imul word ptr DS:[0x004A]
        int result_F000_0124_F0124 = Alu16.Imul(unchecked((short)AX), unchecked((short)UInt16[DS, (ushort)0x004A]));
        DX = unchecked((ushort)(result_F000_0124_F0124 >> 16));
        AX = unchecked((ushort)result_F000_0124_F0124);
        // F000:0128 mov word ptr DS:[0x004C],AX
        UInt16[DS, (ushort)0x004C] = AX;
        // F000:012B mov word ptr DS:[0x004E],DX
        UInt16[DS, (ushort)0x004E] = DX;
        // F000:012F pushf
        Stack.Push16(FlagRegister16);
        // F000:0130 mov AX,0x46DB
        AX = (ushort)0x46DB;
        // F000:0133 mov BP,0
        BP = (ushort)0x0000;
        // F000:0136 imul BP
        int result_F000_0136_F0136 = Alu16.Imul(unchecked((short)AX), unchecked((short)BP));
        DX = unchecked((ushort)(result_F000_0136_F0136 >> 16));
        AX = unchecked((ushort)result_F000_0136_F0136);
        // F000:0138 mov word ptr DS:[0x0050],BP
        UInt16[DS, (ushort)0x0050] = BP;
        // F000:013C mov word ptr DS:[0x0052],AX
        UInt16[DS, (ushort)0x0052] = AX;
        // F000:013F mov word ptr DS:[0x0054],DX
        UInt16[DS, (ushort)0x0054] = DX;
        // F000:0143 pushf
        Stack.Push16(FlagRegister16);
        // F000:0144 mov AX,0x46DB
        AX = (ushort)0x46DB;
        // F000:0147 mov SI,0xEEEB
        SI = (ushort)0xEEEB;
        // F000:014A imul SI
        int result_F000_014A_F014A = Alu16.Imul(unchecked((short)AX), unchecked((short)SI));
        DX = unchecked((ushort)(result_F000_014A_F014A >> 16));
        AX = unchecked((ushort)result_F000_014A_F014A);
        // F000:014C mov word ptr DS:[0x0056],SI
        UInt16[DS, (ushort)0x0056] = SI;
        // F000:0150 mov word ptr DS:[0x0058],AX
        UInt16[DS, (ushort)0x0058] = AX;
        // F000:0153 mov word ptr DS:[0x005A],DX
        UInt16[DS, (ushort)0x005A] = DX;
        // F000:0157 pushf
        Stack.Push16(FlagRegister16);
        // F000:0158 mov BL,0x14
        BL = (byte)0x14;
        // F000:015A mov AX,0xFF07
        AX = (ushort)0xFF07;
        // F000:015D mov DX,0xFFFF
        DX = (ushort)0xFFFF;
        // F000:0160 imul BL
        short result_F000_0160_F0160 = Alu8.Imul(unchecked((sbyte)AL), unchecked((sbyte)BL));
        AH = unchecked((byte)(result_F000_0160_F0160 >> 8));
        AL = unchecked((byte)result_F000_0160_F0160);
        // F000:0162 mov word ptr DS:[0x005C],AX
        UInt16[DS, (ushort)0x005C] = AX;
        // F000:0165 mov word ptr DS:[0x005E],DX
        UInt16[DS, (ushort)0x005E] = DX;
        // F000:0169 pushf
        Stack.Push16(FlagRegister16);
        // F000:016A mov CH,0x24
        CH = (byte)0x24;
        // F000:016C mov AX,0x00FF
        AX = (ushort)0x00FF;
        // F000:016F imul CH
        short result_F000_016F_F016F = Alu8.Imul(unchecked((sbyte)AL), unchecked((sbyte)CH));
        AH = unchecked((byte)(result_F000_016F_F016F >> 8));
        AL = unchecked((byte)result_F000_016F_F016F);
        // F000:0171 mov word ptr DS:[0x0060],AX
        UInt16[DS, (ushort)0x0060] = AX;
        // F000:0174 mov word ptr DS:[0x0062],DX
        UInt16[DS, (ushort)0x0062] = DX;
        // F000:0178 pushf
        Stack.Push16(FlagRegister16);
        // F000:0179 mov AX,0x00FF
        AX = (ushort)0x00FF;
        // F000:017C mov byte ptr DS:[0x0064],1
        UInt8[DS, (ushort)0x0064] = (byte)0x01;
        // F000:0181 imul byte ptr DS:[0x0064]
        short result_F000_0181_F0181 = Alu8.Imul(unchecked((sbyte)AL), unchecked((sbyte)UInt8[DS, (ushort)0x0064]));
        AH = unchecked((byte)(result_F000_0181_F0181 >> 8));
        AL = unchecked((byte)result_F000_0181_F0181);
        // F000:0185 mov word ptr DS:[0x0065],AX
        UInt16[DS, (ushort)0x0065] = AX;
        // F000:0188 mov word ptr DS:[0x0067],DX
        UInt16[DS, (ushort)0x0067] = DX;
        // F000:018C pushf
        Stack.Push16(FlagRegister16);
        // F000:018D mov AX,0xFFFF
        AX = (ushort)0xFFFF;
        // F000:0190 mov byte ptr DS:[0x0069],0xFF
        UInt8[DS, (ushort)0x0069] = (byte)0xFF;
        // F000:0195 imul byte ptr DS:[0x0069]
        short result_F000_0195_F0195 = Alu8.Imul(unchecked((sbyte)AL), unchecked((sbyte)UInt8[DS, (ushort)0x0069]));
        AH = unchecked((byte)(result_F000_0195_F0195 >> 8));
        AL = unchecked((byte)result_F000_0195_F0195);
        // F000:0199 mov word ptr DS:[0x006A],AX
        UInt16[DS, (ushort)0x006A] = AX;
        // F000:019C mov word ptr DS:[0x006A],DX
        UInt16[DS, (ushort)0x006A] = DX;
        // F000:01A0 pushf
        Stack.Push16(FlagRegister16);
        // F000:01A1 mov AX,0x00C5
        AX = (ushort)0x00C5;
        // F000:01A4 mov DX,0
        DX = (ushort)0x0000;
        // F000:01A7 imul DL
        short result_F000_01A7_F01A7 = Alu8.Imul(unchecked((sbyte)AL), unchecked((sbyte)DL));
        AH = unchecked((byte)(result_F000_01A7_F01A7 >> 8));
        AL = unchecked((byte)result_F000_01A7_F01A7);
        // F000:01A9 mov word ptr DS:[0x006C],DX
        UInt16[DS, (ushort)0x006C] = DX;
        // F000:01AD mov word ptr DS:[0x006E],AX
        UInt16[DS, (ushort)0x006E] = AX;
        // F000:01B0 pushf
        Stack.Push16(FlagRegister16);
        // F000:01B1 mov AL,0xB5
        AL = (byte)0xB5;
        // F000:01B3 mov DH,0xF9
        DH = (byte)0xF9;
        // F000:01B5 imul DH
        short result_F000_01B5_F01B5 = Alu8.Imul(unchecked((sbyte)AL), unchecked((sbyte)DH));
        AH = unchecked((byte)(result_F000_01B5_F01B5 >> 8));
        AL = unchecked((byte)result_F000_01B5_F01B5);
        // F000:01B7 mov word ptr DS:[0x0070],SI
        UInt16[DS, (ushort)0x0070] = SI;
        // F000:01BB mov word ptr DS:[0x0072],AX
        UInt16[DS, (ushort)0x0072] = AX;
        // F000:01BE mov word ptr DS:[0x0074],DX
        UInt16[DS, (ushort)0x0074] = DX;
        // F000:01C2 pushf
        Stack.Push16(FlagRegister16);
        // F000:01C3 mov AX,0xFF00
        AX = (ushort)0xFF00;
        // F000:01C6 aad
        byte v2_F000_01C6_F01C6 = (byte)0x0A;
        AL = unchecked((byte)(AL + AH * v2_F000_01C6_F01C6));
        AH = (byte)0x00;
        Alu8.UpdateFlags(AL);
        CarryFlag = false;
        AuxiliaryFlag = false;
        OverflowFlag = false;
        // F000:01C8 mov word ptr DS:[0x0076],AX
        UInt16[DS, (ushort)0x0076] = AX;
        // F000:01CB pushf
        Stack.Push16(FlagRegister16);
        // F000:01CC mov AX,0xFFFF
        AX = (ushort)0xFFFF;
        // F000:01CF aad
        byte v2_F000_01CF_F01CF = (byte)0x12;
        AL = unchecked((byte)(AL + AH * v2_F000_01CF_F01CF));
        AH = (byte)0x00;
        Alu8.UpdateFlags(AL);
        CarryFlag = false;
        AuxiliaryFlag = false;
        OverflowFlag = false;
        // F000:01D1 mov word ptr DS:[0x0078],AX
        UInt16[DS, (ushort)0x0078] = AX;
        // F000:01D4 pushf
        Stack.Push16(FlagRegister16);
        // F000:01D5 mov AX,0x00FF
        AX = (ushort)0x00FF;
        // F000:01D8 aad
        byte v2_F000_01D8_F01D8 = (byte)0xFF;
        AL = unchecked((byte)(AL + AH * v2_F000_01D8_F01D8));
        AH = (byte)0x00;
        Alu8.UpdateFlags(AL);
        CarryFlag = false;
        AuxiliaryFlag = false;
        OverflowFlag = false;
        // F000:01DA mov word ptr DS:[0x007A],AX
        UInt16[DS, (ushort)0x007A] = AX;
        // F000:01DD pushf
        Stack.Push16(FlagRegister16);
        // F000:01DE mov AX,0x532D
        AX = (ushort)0x532D;
        // F000:01E1 aad
        byte v2_F000_01E1_F01E1 = (byte)0x39;
        AL = unchecked((byte)(AL + AH * v2_F000_01E1_F01E1));
        AH = (byte)0x00;
        Alu8.UpdateFlags(AL);
        CarryFlag = false;
        AuxiliaryFlag = false;
        OverflowFlag = false;
        // F000:01E3 mov word ptr DS:[0x007C],AX
        UInt16[DS, (ushort)0x007C] = AX;
        // F000:01E6 pushf
        Stack.Push16(FlagRegister16);
        // F000:01E7 hlt
        return Hlt();
    label_F000_FFF0_FFFF0_0:
        CheckExternalEvents(cs1, 0xFFF0);
        // F000:FFF0 jmp short 0
        goto label_F000_0000_F0000_1;
    }

}
