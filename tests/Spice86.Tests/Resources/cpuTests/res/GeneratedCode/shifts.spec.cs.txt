using Spice86.Core.CLI;
using Spice86.Core.Emulator.CPU.Exceptions;
using Spice86.Core.Emulator.Function;
using Spice86.Core.Emulator.ReverseEngineer;
using Spice86.Core.Emulator.VM;
using Microsoft.Extensions.Logging;
using Spice86.Shared.Emulator.Memory;
using System;
using System.Collections.Generic;

namespace Spice86.Generated;

public sealed class CfgGeneratedOverrideSupplier : IOverrideSupplier {
    public IDictionary<SegmentedAddress, FunctionInformation> GenerateFunctionInformations(ILogger loggerService, Configuration configuration, ushort programStartAddress, Machine machine) {
        return new CfgGeneratedOverrides(new Dictionary<SegmentedAddress, FunctionInformation>(), machine, loggerService, configuration, programStartAddress).FunctionInformations;
    }
}

public class CfgGeneratedOverrides : CSharpOverrideHelper {
    protected readonly ushort cs1;

    public CfgGeneratedOverrides(IDictionary<SegmentedAddress, FunctionInformation> functionInformations, Machine machine, ILogger loggerService, Configuration configuration, ushort programStartSegment) : base(functionInformations, machine, loggerService, configuration) {
        cs1 = 0xF000;

        DefineFunction(cs1, 0xFFF0, unknown_F000_FFF0_FFFF0);
    }

    public virtual Action unknown_F000_FFF0_FFFF0(int loadOffset) {
        goto label_F000_FFF0_FFFF0_0;

    label_F000_0000_F0000_1:
        CheckExternalEvents(cs1, 0x0000);
        // F000:0000 mov AX,0x6EC5
        AX = (ushort)0x6EC5;
        // F000:0003 mov BX,0xB1A8
        BX = (ushort)0xB1A8;
        // F000:0006 mov word ptr DS:[0],0x5338
        UInt16[DS, (ushort)0x0000] = (ushort)0x5338;
        // F000:000C mov word ptr DS:[2],0x31FE
        UInt16[DS, (ushort)0x0002] = (ushort)0x31FE;
        // F000:0012 mov SP,0x0080
        SP = (ushort)0x0080;
        // F000:0015 shl AX,1
        AX = Alu16.Shl(AX, 1);
        // F000:0017 pushf
        Stack.Push16(FlagRegister16);
        // F000:0018 mov word ptr DS:[0x0020],AX
        UInt16[DS, (ushort)0x0020] = AX;
        // F000:001B shl word ptr DS:[0],1
        UInt16[DS, (ushort)0x0000] = Alu16.Shl(UInt16[DS, (ushort)0x0000], 1);
        // F000:001F pushf
        Stack.Push16(FlagRegister16);
        // F000:0020 mov CX,0x0100
        CX = (ushort)0x0100;
        // F000:0023 shl BX,CL
        BX = Alu16.Shl(BX, unchecked((int)CL));
        // F000:0025 pushf
        Stack.Push16(FlagRegister16);
        // F000:0026 mov word ptr DS:[0x0022],BX
        UInt16[DS, (ushort)0x0022] = BX;
        // F000:002A mov CX,0xFFFF
        CX = (ushort)0xFFFF;
        // F000:002D mov DX,BX
        DX = BX;
        // F000:002F shl DX,CL
        DX = Alu16.Shl(DX, unchecked((int)CL));
        // F000:0031 pushf
        Stack.Push16(FlagRegister16);
        // F000:0032 mov word ptr DS:[0x0024],DX
        UInt16[DS, (ushort)0x0024] = DX;
        // F000:0036 mov CL,8
        CL = (byte)0x08;
        // F000:0038 shl BX,CL
        BX = Alu16.Shl(BX, unchecked((int)CL));
        // F000:003A pushf
        Stack.Push16(FlagRegister16);
        // F000:003B mov word ptr DS:[0x0026],BX
        UInt16[DS, (ushort)0x0026] = BX;
        // F000:003F mov CL,4
        CL = (byte)0x04;
        // F000:0041 shl byte ptr DS:[2],CL
        UInt8[DS, (ushort)0x0002] = Alu8.Shl(UInt8[DS, (ushort)0x0002], unchecked((int)CL));
        // F000:0045 pushf
        Stack.Push16(FlagRegister16);
        // F000:0046 mov DX,0x956F
        DX = (ushort)0x956F;
        // F000:0049 mov AX,0x4293
        AX = (ushort)0x4293;
        // F000:004C mov word ptr DS:[4],0x33C0
        UInt16[DS, (ushort)0x0004] = (ushort)0x33C0;
        // F000:0052 mov word ptr DS:[6],0x64FF
        UInt16[DS, (ushort)0x0006] = (ushort)0x64FF;
        // F000:0058 shl AH,1
        AH = Alu8.Shl(AH, 1);
        // F000:005A pushf
        Stack.Push16(FlagRegister16);
        // F000:005B mov word ptr DS:[0x0028],AX
        UInt16[DS, (ushort)0x0028] = AX;
        // F000:005E shl byte ptr DS:[5],1
        UInt8[DS, (ushort)0x0005] = Alu8.Shl(UInt8[DS, (ushort)0x0005], 1);
        // F000:0062 pushf
        Stack.Push16(FlagRegister16);
        // F000:0063 mov CL,7
        CL = (byte)0x07;
        // F000:0065 shl DL,CL
        DL = Alu8.Shl(DL, unchecked((int)CL));
        // F000:0067 pushf
        Stack.Push16(FlagRegister16);
        // F000:0068 mov word ptr DS:[0x002A],DX
        UInt16[DS, (ushort)0x002A] = DX;
        // F000:006C shl byte ptr DS:[6],CL
        UInt8[DS, (ushort)0x0006] = Alu8.Shl(UInt8[DS, (ushort)0x0006], unchecked((int)CL));
        // F000:0070 pushf
        Stack.Push16(FlagRegister16);
        // F000:0071 mov AX,0xFB72
        AX = (ushort)0xFB72;
        // F000:0074 mov BX,0xDFB9
        BX = (ushort)0xDFB9;
        // F000:0077 mov word ptr DS:[8],0x1EBB
        UInt16[DS, (ushort)0x0008] = (ushort)0x1EBB;
        // F000:007D mov word ptr DS:[0x000A],0x742F
        UInt16[DS, (ushort)0x000A] = (ushort)0x742F;
        // F000:0083 sar AX,1
        AX = Alu16.Sar(AX, 1);
        // F000:0085 pushf
        Stack.Push16(FlagRegister16);
        // F000:0086 mov word ptr DS:[0x002C],AX
        UInt16[DS, (ushort)0x002C] = AX;
        // F000:0089 sar word ptr DS:[8],1
        UInt16[DS, (ushort)0x0008] = Alu16.Sar(UInt16[DS, (ushort)0x0008], 1);
        // F000:008D pushf
        Stack.Push16(FlagRegister16);
        // F000:008E mov CX,0x0100
        CX = (ushort)0x0100;
        // F000:0091 sar BX,CL
        BX = Alu16.Sar(BX, unchecked((int)CL));
        // F000:0093 pushf
        Stack.Push16(FlagRegister16);
        // F000:0094 mov word ptr DS:[0x002E],BX
        UInt16[DS, (ushort)0x002E] = BX;
        // F000:0098 mov CX,0xFFFF
        CX = (ushort)0xFFFF;
        // F000:009B mov DX,BX
        DX = BX;
        // F000:009D sar DX,CL
        DX = Alu16.Sar(DX, unchecked((int)CL));
        // F000:009F pushf
        Stack.Push16(FlagRegister16);
        // F000:00A0 mov word ptr DS:[0x0030],DX
        UInt16[DS, (ushort)0x0030] = DX;
        // F000:00A4 mov CL,5
        CL = (byte)0x05;
        // F000:00A6 sar BX,CL
        BX = Alu16.Sar(BX, unchecked((int)CL));
        // F000:00A8 pushf
        Stack.Push16(FlagRegister16);
        // F000:00A9 mov word ptr DS:[0x0032],BX
        UInt16[DS, (ushort)0x0032] = BX;
        // F000:00AD mov CL,4
        CL = (byte)0x04;
        // F000:00AF sar byte ptr DS:[0x000A],CL
        UInt8[DS, (ushort)0x000A] = Alu8.Sar(UInt8[DS, (ushort)0x000A], unchecked((int)CL));
        // F000:00B3 pushf
        Stack.Push16(FlagRegister16);
        // F000:00B4 mov DX,0x93B8
        DX = (ushort)0x93B8;
        // F000:00B7 mov AX,0x6688
        AX = (ushort)0x6688;
        // F000:00BA mov word ptr DS:[0x000C],0xCAD4
        UInt16[DS, (ushort)0x000C] = (ushort)0xCAD4;
        // F000:00C0 mov word ptr DS:[0x000E],0x6EC9
        UInt16[DS, (ushort)0x000E] = (ushort)0x6EC9;
        // F000:00C6 sar AH,1
        AH = Alu8.Sar(AH, 1);
        // F000:00C8 pushf
        Stack.Push16(FlagRegister16);
        // F000:00C9 mov word ptr DS:[0x0034],AX
        UInt16[DS, (ushort)0x0034] = AX;
        // F000:00CC sar byte ptr DS:[0x000D],1
        UInt8[DS, (ushort)0x000D] = Alu8.Sar(UInt8[DS, (ushort)0x000D], 1);
        // F000:00D0 pushf
        Stack.Push16(FlagRegister16);
        // F000:00D1 mov CL,7
        CL = (byte)0x07;
        // F000:00D3 sar DL,CL
        DL = Alu8.Sar(DL, unchecked((int)CL));
        // F000:00D5 pushf
        Stack.Push16(FlagRegister16);
        // F000:00D6 mov word ptr DS:[0x0036],DX
        UInt16[DS, (ushort)0x0036] = DX;
        // F000:00DA sar byte ptr DS:[0x000E],CL
        UInt8[DS, (ushort)0x000E] = Alu8.Sar(UInt8[DS, (ushort)0x000E], unchecked((int)CL));
        // F000:00DE pushf
        Stack.Push16(FlagRegister16);
        // F000:00DF mov AX,0x7BA1
        AX = (ushort)0x7BA1;
        // F000:00E2 mov BX,0x54E8
        BX = (ushort)0x54E8;
        // F000:00E5 mov word ptr DS:[0x0010],0xBAAA
        UInt16[DS, (ushort)0x0010] = (ushort)0xBAAA;
        // F000:00EB mov word ptr DS:[0x0012],0x3431
        UInt16[DS, (ushort)0x0012] = (ushort)0x3431;
        // F000:00F1 shr AX,1
        AX = Alu16.Shr(AX, 1);
        // F000:00F3 pushf
        Stack.Push16(FlagRegister16);
        // F000:00F4 mov word ptr DS:[0x0038],AX
        UInt16[DS, (ushort)0x0038] = AX;
        // F000:00F7 shr word ptr DS:[0x0010],1
        UInt16[DS, (ushort)0x0010] = Alu16.Shr(UInt16[DS, (ushort)0x0010], 1);
        // F000:00FB pushf
        Stack.Push16(FlagRegister16);
        // F000:00FC mov CX,0x0100
        CX = (ushort)0x0100;
        // F000:00FF shr BX,CL
        BX = Alu16.Shr(BX, unchecked((int)CL));
        // F000:0101 pushf
        Stack.Push16(FlagRegister16);
        // F000:0102 mov word ptr DS:[0x003A],BX
        UInt16[DS, (ushort)0x003A] = BX;
        // F000:0106 mov CX,0xFFFF
        CX = (ushort)0xFFFF;
        // F000:0109 mov DX,BX
        DX = BX;
        // F000:010B shr DX,CL
        DX = Alu16.Shr(DX, unchecked((int)CL));
        // F000:010D pushf
        Stack.Push16(FlagRegister16);
        // F000:010E mov word ptr DS:[0x003C],DX
        UInt16[DS, (ushort)0x003C] = DX;
        // F000:0112 mov CL,4
        CL = (byte)0x04;
        // F000:0114 shr BX,CL
        BX = Alu16.Shr(BX, unchecked((int)CL));
        // F000:0116 pushf
        Stack.Push16(FlagRegister16);
        // F000:0117 mov word ptr DS:[0x003E],BX
        UInt16[DS, (ushort)0x003E] = BX;
        // F000:011B mov CL,4
        CL = (byte)0x04;
        // F000:011D shr byte ptr DS:[0x0012],CL
        UInt8[DS, (ushort)0x0012] = Alu8.Shr(UInt8[DS, (ushort)0x0012], unchecked((int)CL));
        // F000:0121 pushf
        Stack.Push16(FlagRegister16);
        // F000:0122 mov DX,0x0410
        DX = (ushort)0x0410;
        // F000:0125 mov AX,0x1628
        AX = (ushort)0x1628;
        // F000:0128 mov word ptr DS:[0x0014],0x3B26
        UInt16[DS, (ushort)0x0014] = (ushort)0x3B26;
        // F000:012E mov word ptr DS:[0x0016],0x8D0D
        UInt16[DS, (ushort)0x0016] = (ushort)0x8D0D;
        // F000:0134 shr AH,1
        AH = Alu8.Shr(AH, 1);
        // F000:0136 pushf
        Stack.Push16(FlagRegister16);
        // F000:0137 mov word ptr DS:[0x0040],AX
        UInt16[DS, (ushort)0x0040] = AX;
        // F000:013A shr byte ptr DS:[0x0015],1
        UInt8[DS, (ushort)0x0015] = Alu8.Shr(UInt8[DS, (ushort)0x0015], 1);
        // F000:013E pushf
        Stack.Push16(FlagRegister16);
        // F000:013F mov CL,7
        CL = (byte)0x07;
        // F000:0141 shr DL,CL
        DL = Alu8.Shr(DL, unchecked((int)CL));
        // F000:0143 pushf
        Stack.Push16(FlagRegister16);
        // F000:0144 mov word ptr DS:[0x0042],DX
        UInt16[DS, (ushort)0x0042] = DX;
        // F000:0148 shr byte ptr DS:[0x0016],CL
        UInt8[DS, (ushort)0x0016] = Alu8.Shr(UInt8[DS, (ushort)0x0016], unchecked((int)CL));
        // F000:014C pushf
        Stack.Push16(FlagRegister16);
        // F000:014D hlt
        return Hlt();
    label_F000_FFF0_FFFF0_0:
        CheckExternalEvents(cs1, 0xFFF0);
        // F000:FFF0 jmp short 0
        goto label_F000_0000_F0000_1;
    }

}
