using Spice86.Core.CLI;
using Spice86.Core.Emulator.CPU.Exceptions;
using Spice86.Core.Emulator.Function;
using Spice86.Core.Emulator.ReverseEngineer;
using Spice86.Core.Emulator.VM;
using Microsoft.Extensions.Logging;
using Spice86.Shared.Emulator.Memory;
using System;
using System.Collections.Generic;

namespace Spice86.Generated;

public sealed class CfgGeneratedOverrideSupplier : IOverrideSupplier {
    public IDictionary<SegmentedAddress, FunctionInformation> GenerateFunctionInformations(ILogger loggerService, Configuration configuration, ushort programStartAddress, Machine machine) {
        return new CfgGeneratedOverrides(new Dictionary<SegmentedAddress, FunctionInformation>(), machine, loggerService, configuration, programStartAddress).FunctionInformations;
    }
}

public class CfgGeneratedOverrides : CSharpOverrideHelper {
    protected readonly ushort cs1;

    public CfgGeneratedOverrides(IDictionary<SegmentedAddress, FunctionInformation> functionInformations, Machine machine, ILogger loggerService, Configuration configuration, ushort programStartSegment) : base(functionInformations, machine, loggerService, configuration) {
        cs1 = 0xF000;

        DefineFunction(cs1, 0xFFF0, unknown_F000_FFF0_FFFF0);
    }

    public virtual Action unknown_F000_FFF0_FFFF0(int loadOffset) {
        goto label_F000_FFF0_FFFF0_0;

    label_F000_0000_F0000_1:
        CheckExternalEvents(cs1, 0x0000);
        // F000:0000 mov SP,0x00A0
        SP = (ushort)0x00A0;
        // F000:0003 mov AX,0xFFFF
        AX = (ushort)0xFFFF;
        // F000:0006 mov BX,1
        BX = (ushort)0x0001;
        // F000:0009 add BX,AX
        BX = Alu16.Add(BX, AX);
        // F000:000B mov word ptr DS:[0],AX
        UInt16[DS, (ushort)0x0000] = AX;
        // F000:000E mov word ptr DS:[2],BX
        UInt16[DS, (ushort)0x0002] = BX;
        // F000:0012 pushf
        Stack.Push16(FlagRegister16);
        // F000:0013 mov DX,0xFFFF
        DX = (ushort)0xFFFF;
        // F000:0016 mov word ptr DS:[4],0xFFFF
        UInt16[DS, (ushort)0x0004] = (ushort)0xFFFF;
        // F000:001C add word ptr DS:[4],DX
        UInt16[DS, (ushort)0x0004] = Alu16.Add(UInt16[DS, (ushort)0x0004], DX);
        // F000:0020 mov word ptr DS:[6],DX
        UInt16[DS, (ushort)0x0006] = DX;
        // F000:0024 pushf
        Stack.Push16(FlagRegister16);
        // F000:0025 mov CX,1
        CX = (ushort)0x0001;
        // F000:0028 mov word ptr DS:[8],2
        UInt16[DS, (ushort)0x0008] = (ushort)0x0002;
        // F000:002E add CX,word ptr DS:[8]
        CX = Alu16.Add(CX, UInt16[DS, (ushort)0x0008]);
        // F000:0032 mov word ptr DS:[0x000A],CX
        UInt16[DS, (ushort)0x000A] = CX;
        // F000:0036 pushf
        Stack.Push16(FlagRegister16);
        // F000:0037 mov AX,1
        AX = (ushort)0x0001;
        // F000:003A add AX,0x7FFF
        AX = Alu16.Add(AX, (ushort)0x7FFF);
        // F000:003D mov word ptr DS:[0x000C],AX
        UInt16[DS, (ushort)0x000C] = AX;
        // F000:0040 pushf
        Stack.Push16(FlagRegister16);
        // F000:0041 mov BP,0x8000
        BP = (ushort)0x8000;
        // F000:0044 add BP,-1
        BP = Alu16.Add(BP, unchecked((ushort)unchecked((short)(sbyte)-1)));
        // F000:0047 mov word ptr DS:[0x000E],BP
        UInt16[DS, (ushort)0x000E] = BP;
        // F000:004B pushf
        Stack.Push16(FlagRegister16);
        // F000:004C mov SI,0xC783
        SI = (ushort)0xC783;
        // F000:004F add SI,0xEB2A
        SI = Alu16.Add(SI, (ushort)0xEB2A);
        // F000:0053 mov word ptr DS:[0x0010],SI
        UInt16[DS, (ushort)0x0010] = SI;
        // F000:0057 pushf
        Stack.Push16(FlagRegister16);
        // F000:0058 mov word ptr DS:[0x0012],0x8960
        UInt16[DS, (ushort)0x0012] = (ushort)0x8960;
        // F000:005E add word ptr DS:[0x0012],0x0A95
        UInt16[DS, (ushort)0x0012] = Alu16.Add(UInt16[DS, (ushort)0x0012], (ushort)0x0A95);
        // F000:0064 pushf
        Stack.Push16(FlagRegister16);
        // F000:0065 mov word ptr DS:[0x0014],0xF1E1
        UInt16[DS, (ushort)0x0014] = (ushort)0xF1E1;
        // F000:006B add word ptr DS:[0x0014],0x0064
        UInt16[DS, (ushort)0x0014] = Alu16.Add(UInt16[DS, (ushort)0x0014], unchecked((ushort)unchecked((short)(sbyte)100)));
        // F000:0070 pushf
        Stack.Push16(FlagRegister16);
        // F000:0071 mov byte ptr DS:[0x0016],1
        UInt8[DS, (ushort)0x0016] = (byte)0x01;
        // F000:0076 add byte ptr DS:[0x0016],0xFF
        UInt8[DS, (ushort)0x0016] = Alu8.Add(UInt8[DS, (ushort)0x0016], (byte)0xFF);
        // F000:007B pushf
        Stack.Push16(FlagRegister16);
        // F000:007C mov DH,0xFF
        DH = (byte)0xFF;
        // F000:007E add DH,0xFF
        DH = Alu8.Add(DH, (byte)0xFF);
        // F000:0081 mov word ptr DS:[0x0017],DX
        UInt16[DS, (ushort)0x0017] = DX;
        // F000:0085 pushf
        Stack.Push16(FlagRegister16);
        // F000:0086 mov AL,1
        AL = (byte)0x01;
        // F000:0088 add AL,2
        AL = Alu8.Add(AL, (byte)0x02);
        // F000:008A mov word ptr DS:[0x0019],AX
        UInt16[DS, (ushort)0x0019] = AX;
        // F000:008D pushf
        Stack.Push16(FlagRegister16);
        // F000:008E mov byte ptr DS:[0x001B],0x7F
        UInt8[DS, (ushort)0x001B] = (byte)0x7F;
        // F000:0093 mov CH,1
        CH = (byte)0x01;
        // F000:0095 add CH,byte ptr DS:[0x001B]
        CH = Alu8.Add(CH, UInt8[DS, (ushort)0x001B]);
        // F000:0099 mov word ptr DS:[0x001C],CX
        UInt16[DS, (ushort)0x001C] = CX;
        // F000:009D pushf
        Stack.Push16(FlagRegister16);
        // F000:009E mov BL,0x80
        BL = (byte)0x80;
        // F000:00A0 mov byte ptr DS:[0x001E],0xFF
        UInt8[DS, (ushort)0x001E] = (byte)0xFF;
        // F000:00A5 add byte ptr DS:[0x001E],BL
        UInt8[DS, (ushort)0x001E] = Alu8.Add(UInt8[DS, (ushort)0x001E], BL);
        // F000:00A9 mov word ptr DS:[0x001F],BX
        UInt16[DS, (ushort)0x001F] = BX;
        // F000:00AD pushf
        Stack.Push16(FlagRegister16);
        // F000:00AE mov AL,0xA6
        AL = (byte)0xA6;
        // F000:00B0 mov AH,0x86
        AH = (byte)0x86;
        // F000:00B2 add AH,AL
        AH = Alu8.Add(AH, AL);
        // F000:00B4 mov word ptr DS:[0x0021],AX
        UInt16[DS, (ushort)0x0021] = AX;
        // F000:00B7 pushf
        Stack.Push16(FlagRegister16);
        // F000:00B8 mov AX,0xFFFF
        AX = (ushort)0xFFFF;
        // F000:00BB mov BX,1
        BX = (ushort)0x0001;
        // F000:00BE adc BX,AX
        BX = Alu16.Adc(BX, AX);
        // F000:00C0 mov word ptr DS:[0x0023],AX
        UInt16[DS, (ushort)0x0023] = AX;
        // F000:00C3 mov word ptr DS:[0x0025],BX
        UInt16[DS, (ushort)0x0025] = BX;
        // F000:00C7 pushf
        Stack.Push16(FlagRegister16);
        // F000:00C8 mov DX,0xFFFF
        DX = (ushort)0xFFFF;
        // F000:00CB mov word ptr DS:[0x0027],0xFFFF
        UInt16[DS, (ushort)0x0027] = (ushort)0xFFFF;
        // F000:00D1 adc word ptr DS:[0x0027],DX
        UInt16[DS, (ushort)0x0027] = Alu16.Adc(UInt16[DS, (ushort)0x0027], DX);
        // F000:00D5 mov word ptr DS:[0x0029],DX
        UInt16[DS, (ushort)0x0029] = DX;
        // F000:00D9 pushf
        Stack.Push16(FlagRegister16);
        // F000:00DA mov CX,1
        CX = (ushort)0x0001;
        // F000:00DD mov word ptr DS:[0x002B],2
        UInt16[DS, (ushort)0x002B] = (ushort)0x0002;
        // F000:00E3 adc CX,word ptr DS:[0x002B]
        CX = Alu16.Adc(CX, UInt16[DS, (ushort)0x002B]);
        // F000:00E7 mov word ptr DS:[0x002D],CX
        UInt16[DS, (ushort)0x002D] = CX;
        // F000:00EB pushf
        Stack.Push16(FlagRegister16);
        // F000:00EC mov AX,1
        AX = (ushort)0x0001;
        // F000:00EF adc AX,0x7FFF
        AX = Alu16.Adc(AX, (ushort)0x7FFF);
        // F000:00F2 mov word ptr DS:[0x002F],AX
        UInt16[DS, (ushort)0x002F] = AX;
        // F000:00F5 pushf
        Stack.Push16(FlagRegister16);
        // F000:00F6 mov BP,0x8000
        BP = (ushort)0x8000;
        // F000:00F9 adc BP,-1
        BP = Alu16.Adc(BP, unchecked((ushort)unchecked((short)(sbyte)-1)));
        // F000:00FC mov word ptr DS:[0x0031],BP
        UInt16[DS, (ushort)0x0031] = BP;
        // F000:0100 pushf
        Stack.Push16(FlagRegister16);
        // F000:0101 mov SI,0x77D3
        SI = (ushort)0x77D3;
        // F000:0104 adc SI,0x8425
        SI = Alu16.Adc(SI, (ushort)0x8425);
        // F000:0108 mov word ptr DS:[0x0033],SI
        UInt16[DS, (ushort)0x0033] = SI;
        // F000:010C pushf
        Stack.Push16(FlagRegister16);
        // F000:010D mov word ptr DS:[0x0035],0xEBA0
        UInt16[DS, (ushort)0x0035] = (ushort)0xEBA0;
        // F000:0113 adc word ptr DS:[0x0035],0xD3C1
        UInt16[DS, (ushort)0x0035] = Alu16.Adc(UInt16[DS, (ushort)0x0035], (ushort)0xD3C1);
        // F000:0119 pushf
        Stack.Push16(FlagRegister16);
        // F000:011A mov word ptr DS:[0x0037],0x7F50
        UInt16[DS, (ushort)0x0037] = (ushort)0x7F50;
        // F000:0120 adc word ptr DS:[0x0037],-11
        UInt16[DS, (ushort)0x0037] = Alu16.Adc(UInt16[DS, (ushort)0x0037], unchecked((ushort)unchecked((short)(sbyte)-11)));
        // F000:0125 pushf
        Stack.Push16(FlagRegister16);
        // F000:0126 mov byte ptr DS:[0x0039],1
        UInt8[DS, (ushort)0x0039] = (byte)0x01;
        // F000:012B adc byte ptr DS:[0x0039],0xFF
        UInt8[DS, (ushort)0x0039] = Alu8.Adc(UInt8[DS, (ushort)0x0039], (byte)0xFF);
        // F000:0130 pushf
        Stack.Push16(FlagRegister16);
        // F000:0131 mov DH,0xFF
        DH = (byte)0xFF;
        // F000:0133 adc DH,0xFF
        DH = Alu8.Adc(DH, (byte)0xFF);
        // F000:0136 mov word ptr DS:[0x003A],DX
        UInt16[DS, (ushort)0x003A] = DX;
        // F000:013A pushf
        Stack.Push16(FlagRegister16);
        // F000:013B mov AL,1
        AL = (byte)0x01;
        // F000:013D adc AL,2
        AL = Alu8.Adc(AL, (byte)0x02);
        // F000:013F mov word ptr DS:[0x003C],AX
        UInt16[DS, (ushort)0x003C] = AX;
        // F000:0142 pushf
        Stack.Push16(FlagRegister16);
        // F000:0143 mov byte ptr DS:[0x003E],0x7F
        UInt8[DS, (ushort)0x003E] = (byte)0x7F;
        // F000:0148 mov CH,1
        CH = (byte)0x01;
        // F000:014A adc CH,byte ptr DS:[0x003E]
        CH = Alu8.Adc(CH, UInt8[DS, (ushort)0x003E]);
        // F000:014E mov word ptr DS:[0x003F],CX
        UInt16[DS, (ushort)0x003F] = CX;
        // F000:0152 pushf
        Stack.Push16(FlagRegister16);
        // F000:0153 mov BL,0x80
        BL = (byte)0x80;
        // F000:0155 mov byte ptr DS:[0x0041],0xFF
        UInt8[DS, (ushort)0x0041] = (byte)0xFF;
        // F000:015A adc byte ptr DS:[0x0041],BL
        UInt8[DS, (ushort)0x0041] = Alu8.Adc(UInt8[DS, (ushort)0x0041], BL);
        // F000:015E mov word ptr DS:[0x0042],BX
        UInt16[DS, (ushort)0x0042] = BX;
        // F000:0162 pushf
        Stack.Push16(FlagRegister16);
        // F000:0163 mov AL,0xB9
        AL = (byte)0xB9;
        // F000:0165 mov AH,0xD3
        AH = (byte)0xD3;
        // F000:0167 adc AH,AL
        AH = Alu8.Adc(AH, AL);
        // F000:0169 mov word ptr DS:[0x0044],AX
        UInt16[DS, (ushort)0x0044] = AX;
        // F000:016C pushf
        Stack.Push16(FlagRegister16);
        // F000:016D mov DI,0xFFFF
        DI = (ushort)0xFFFF;
        // F000:0170 inc DI
        DI = Alu16.Inc(DI);
        // F000:0171 mov word ptr DS:[0x0046],DI
        UInt16[DS, (ushort)0x0046] = DI;
        // F000:0175 pushf
        Stack.Push16(FlagRegister16);
        // F000:0176 mov BP,0x7FFF
        BP = (ushort)0x7FFF;
        // F000:0179 inc BP
        BP = Alu16.Inc(BP);
        // F000:017B mov word ptr DS:[0x0048],BP
        UInt16[DS, (ushort)0x0048] = BP;
        // F000:017F pushf
        Stack.Push16(FlagRegister16);
        // F000:0180 mov word ptr DS:[0x004A],0x7412
        UInt16[DS, (ushort)0x004A] = (ushort)0x7412;
        // F000:0186 inc word ptr DS:[0x004A]
        UInt16[DS, (ushort)0x004A] = Alu16.Inc(UInt16[DS, (ushort)0x004A]);
        // F000:018A pushf
        Stack.Push16(FlagRegister16);
        // F000:018B mov DL,0x7F
        DL = (byte)0x7F;
        // F000:018D inc DL
        DL = Alu8.Inc(DL);
        // F000:018F mov word ptr DS:[0x004C],DX
        UInt16[DS, (ushort)0x004C] = DX;
        // F000:0193 pushf
        Stack.Push16(FlagRegister16);
        // F000:0194 mov byte ptr DS:[0x004D],0xFF
        UInt8[DS, (ushort)0x004D] = (byte)0xFF;
        // F000:0199 inc byte ptr DS:[0x004D]
        UInt8[DS, (ushort)0x004D] = Alu8.Inc(UInt8[DS, (ushort)0x004D]);
        // F000:019D pushf
        Stack.Push16(FlagRegister16);
        // F000:019E mov byte ptr DS:[0x004E],0xB5
        UInt8[DS, (ushort)0x004E] = (byte)0xB5;
        // F000:01A3 inc byte ptr DS:[0x004E]
        UInt8[DS, (ushort)0x004E] = Alu8.Inc(UInt8[DS, (ushort)0x004E]);
        // F000:01A7 pushf
        Stack.Push16(FlagRegister16);
        // F000:01A8 hlt
        return Hlt();
    label_F000_FFF0_FFFF0_0:
        CheckExternalEvents(cs1, 0xFFF0);
        // F000:FFF0 jmp short 0
        goto label_F000_0000_F0000_1;
    }

}
