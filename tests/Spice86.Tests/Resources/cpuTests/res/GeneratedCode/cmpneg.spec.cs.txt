using Spice86.Core.CLI;
using Spice86.Core.Emulator.CPU.Exceptions;
using Spice86.Core.Emulator.Function;
using Spice86.Core.Emulator.ReverseEngineer;
using Spice86.Core.Emulator.VM;
using Microsoft.Extensions.Logging;
using Spice86.Shared.Emulator.Memory;
using System;
using System.Collections.Generic;

namespace Spice86.Generated;

public sealed class CfgGeneratedOverrideSupplier : IOverrideSupplier {
    public IDictionary<SegmentedAddress, FunctionInformation> GenerateFunctionInformations(ILogger loggerService, Configuration configuration, ushort programStartAddress, Machine machine) {
        return new CfgGeneratedOverrides(new Dictionary<SegmentedAddress, FunctionInformation>(), machine, loggerService, configuration, programStartAddress).FunctionInformations;
    }
}

public class CfgGeneratedOverrides : CSharpOverrideHelper {
    protected readonly ushort cs1;

    public CfgGeneratedOverrides(IDictionary<SegmentedAddress, FunctionInformation> functionInformations, Machine machine, ILogger loggerService, Configuration configuration, ushort programStartSegment) : base(functionInformations, machine, loggerService, configuration) {
        cs1 = 0xF000;

        DefineFunction(cs1, 0xFFF0, unknown_F000_FFF0_FFFF0);
    }

    public virtual Action unknown_F000_FFF0_FFFF0(int loadOffset) {
        goto label_F000_FFF0_FFFF0_0;

    label_F000_0000_F0000_1:
        CheckExternalEvents(cs1, 0x0000);
        // F000:0000 mov SP,0x0060
        SP = (ushort)0x0060;
        // F000:0003 mov AX,1
        AX = (ushort)0x0001;
        // F000:0006 mov BX,2
        BX = (ushort)0x0002;
        // F000:0009 cmp AX,BX
        Alu16.Sub(AX, BX);
        // F000:000B mov word ptr DS:[0],AX
        UInt16[DS, (ushort)0x0000] = AX;
        // F000:000E mov word ptr DS:[2],BX
        UInt16[DS, (ushort)0x0002] = BX;
        // F000:0012 pushf
        Stack.Push16(FlagRegister16);
        // F000:0013 mov DX,0xFFFF
        DX = (ushort)0xFFFF;
        // F000:0016 mov word ptr DS:[4],0xFFFF
        UInt16[DS, (ushort)0x0004] = (ushort)0xFFFF;
        // F000:001C cmp word ptr DS:[4],DX
        Alu16.Sub(UInt16[DS, (ushort)0x0004], DX);
        // F000:0020 mov word ptr DS:[6],DX
        UInt16[DS, (ushort)0x0006] = DX;
        // F000:0024 pushf
        Stack.Push16(FlagRegister16);
        // F000:0025 mov CX,0xFFFF
        CX = (ushort)0xFFFF;
        // F000:0028 mov word ptr DS:[8],1
        UInt16[DS, (ushort)0x0008] = (ushort)0x0001;
        // F000:002E cmp word ptr DS:[8],CX
        Alu16.Sub(UInt16[DS, (ushort)0x0008], CX);
        // F000:0032 mov word ptr DS:[0x000A],CX
        UInt16[DS, (ushort)0x000A] = CX;
        // F000:0036 pushf
        Stack.Push16(FlagRegister16);
        // F000:0037 mov AX,0x8000
        AX = (ushort)0x8000;
        // F000:003A cmp AX,1
        Alu16.Sub(AX, unchecked((ushort)unchecked((short)(sbyte)1)));
        // F000:003D mov word ptr DS:[0x000C],AX
        UInt16[DS, (ushort)0x000C] = AX;
        // F000:0040 pushf
        Stack.Push16(FlagRegister16);
        // F000:0041 mov BP,0x8000
        BP = (ushort)0x8000;
        // F000:0044 cmp BP,-1
        Alu16.Sub(BP, unchecked((ushort)unchecked((short)(sbyte)-1)));
        // F000:0047 mov word ptr DS:[0x000E],BP
        UInt16[DS, (ushort)0x000E] = BP;
        // F000:004B pushf
        Stack.Push16(FlagRegister16);
        // F000:004C mov SI,0x7F81
        SI = (ushort)0x7F81;
        // F000:004F cmp SI,0x903C
        Alu16.Sub(SI, (ushort)0x903C);
        // F000:0053 mov word ptr DS:[0x0010],SI
        UInt16[DS, (ushort)0x0010] = SI;
        // F000:0057 pushf
        Stack.Push16(FlagRegister16);
        // F000:0058 mov word ptr DS:[0x0012],0xEFC3
        UInt16[DS, (ushort)0x0012] = (ushort)0xEFC3;
        // F000:005E cmp word ptr DS:[0x0012],0xC664
        Alu16.Sub(UInt16[DS, (ushort)0x0012], (ushort)0xC664);
        // F000:0064 pushf
        Stack.Push16(FlagRegister16);
        // F000:0065 mov word ptr DS:[0x0014],0xE933
        UInt16[DS, (ushort)0x0014] = (ushort)0xE933;
        // F000:006B cmp word ptr DS:[0x0014],0x0064
        Alu16.Sub(UInt16[DS, (ushort)0x0014], unchecked((ushort)unchecked((short)(sbyte)100)));
        // F000:0070 pushf
        Stack.Push16(FlagRegister16);
        // F000:0071 mov byte ptr DS:[0x0016],1
        UInt8[DS, (ushort)0x0016] = (byte)0x01;
        // F000:0076 cmp byte ptr DS:[0x0016],2
        Alu8.Sub(UInt8[DS, (ushort)0x0016], (byte)0x02);
        // F000:007B pushf
        Stack.Push16(FlagRegister16);
        // F000:007C mov DH,0xFF
        DH = (byte)0xFF;
        // F000:007E cmp DH,0xFF
        Alu8.Sub(DH, (byte)0xFF);
        // F000:0081 mov word ptr DS:[0x0017],DX
        UInt16[DS, (ushort)0x0017] = DX;
        // F000:0085 pushf
        Stack.Push16(FlagRegister16);
        // F000:0086 mov AL,0xFF
        AL = (byte)0xFF;
        // F000:0088 cmp AL,1
        Alu8.Sub(AL, (byte)0x01);
        // F000:008A mov word ptr DS:[0x0019],AX
        UInt16[DS, (ushort)0x0019] = AX;
        // F000:008D pushf
        Stack.Push16(FlagRegister16);
        // F000:008E mov byte ptr DS:[0x001B],0x80
        UInt8[DS, (ushort)0x001B] = (byte)0x80;
        // F000:0093 mov CH,1
        CH = (byte)0x01;
        // F000:0095 cmp CH,byte ptr DS:[0x001B]
        Alu8.Sub(CH, UInt8[DS, (ushort)0x001B]);
        // F000:0099 mov word ptr DS:[0x001C],CX
        UInt16[DS, (ushort)0x001C] = CX;
        // F000:009D pushf
        Stack.Push16(FlagRegister16);
        // F000:009E mov BL,0x80
        BL = (byte)0x80;
        // F000:00A0 mov byte ptr DS:[0x001E],0x7F
        UInt8[DS, (ushort)0x001E] = (byte)0x7F;
        // F000:00A5 cmp byte ptr DS:[0x001E],BL
        Alu8.Sub(UInt8[DS, (ushort)0x001E], BL);
        // F000:00A9 mov word ptr DS:[0x001F],BX
        UInt16[DS, (ushort)0x001F] = BX;
        // F000:00AD pushf
        Stack.Push16(FlagRegister16);
        // F000:00AE mov AL,0xBC
        AL = (byte)0xBC;
        // F000:00B0 mov AH,0x8E
        AH = (byte)0x8E;
        // F000:00B2 cmp AH,AL
        Alu8.Sub(AH, AL);
        // F000:00B4 mov word ptr DS:[0x0021],AX
        UInt16[DS, (ushort)0x0021] = AX;
        // F000:00B7 pushf
        Stack.Push16(FlagRegister16);
        // F000:00B8 mov CX,0
        CX = (ushort)0x0000;
        // F000:00BB neg CX
        CX = Alu16.Sub((ushort)0x0000, CX);
        CarryFlag = CX != (ushort)0x0000;
        // F000:00BD mov word ptr DS:[0x0022],CX
        UInt16[DS, (ushort)0x0022] = CX;
        // F000:00C1 pushf
        Stack.Push16(FlagRegister16);
        // F000:00C2 mov word ptr DS:[0x0024],0x7FFF
        UInt16[DS, (ushort)0x0024] = (ushort)0x7FFF;
        // F000:00C8 neg word ptr DS:[0x0024]
        UInt16[DS, (ushort)0x0024] = Alu16.Sub((ushort)0x0000, UInt16[DS, (ushort)0x0024]);
        CarryFlag = UInt16[DS, (ushort)0x0024] != (ushort)0x0000;
        // F000:00CC pushf
        Stack.Push16(FlagRegister16);
        // F000:00CD mov BP,0x8000
        BP = (ushort)0x8000;
        // F000:00D0 neg BP
        BP = Alu16.Sub((ushort)0x0000, BP);
        CarryFlag = BP != (ushort)0x0000;
        // F000:00D2 mov word ptr DS:[0x0026],BP
        UInt16[DS, (ushort)0x0026] = BP;
        // F000:00D6 pushf
        Stack.Push16(FlagRegister16);
        // F000:00D7 mov word ptr DS:[0x0028],0xACE9
        UInt16[DS, (ushort)0x0028] = (ushort)0xACE9;
        // F000:00DD neg word ptr DS:[0x0028]
        UInt16[DS, (ushort)0x0028] = Alu16.Sub((ushort)0x0000, UInt16[DS, (ushort)0x0028]);
        CarryFlag = UInt16[DS, (ushort)0x0028] != (ushort)0x0000;
        // F000:00E1 pushf
        Stack.Push16(FlagRegister16);
        // F000:00E2 mov AH,0
        AH = (byte)0x00;
        // F000:00E4 neg AH
        AH = Alu8.Sub((byte)0x00, AH);
        CarryFlag = AH != (byte)0x00;
        // F000:00E6 mov word ptr DS:[0x002A],AX
        UInt16[DS, (ushort)0x002A] = AX;
        // F000:00E9 pushf
        Stack.Push16(FlagRegister16);
        // F000:00EA mov byte ptr DS:[0x002C],0x7F
        UInt8[DS, (ushort)0x002C] = (byte)0x7F;
        // F000:00EF neg byte ptr DS:[0x002C]
        UInt8[DS, (ushort)0x002C] = Alu8.Sub((byte)0x00, UInt8[DS, (ushort)0x002C]);
        CarryFlag = UInt8[DS, (ushort)0x002C] != (byte)0x00;
        // F000:00F3 pushf
        Stack.Push16(FlagRegister16);
        // F000:00F4 mov CL,0xC9
        CL = (byte)0xC9;
        // F000:00F6 neg CL
        CL = Alu8.Sub((byte)0x00, CL);
        CarryFlag = CL != (byte)0x00;
        // F000:00F8 mov word ptr DS:[0x002D],CX
        UInt16[DS, (ushort)0x002D] = CX;
        // F000:00FC pushf
        Stack.Push16(FlagRegister16);
        // F000:00FD mov byte ptr DS:[0x002F],0x80
        UInt8[DS, (ushort)0x002F] = (byte)0x80;
        // F000:0102 neg byte ptr DS:[0x002F]
        UInt8[DS, (ushort)0x002F] = Alu8.Sub((byte)0x00, UInt8[DS, (ushort)0x002F]);
        CarryFlag = UInt8[DS, (ushort)0x002F] != (byte)0x00;
        // F000:0106 pushf
        Stack.Push16(FlagRegister16);
        // F000:0107 hlt
        return Hlt();
    label_F000_FFF0_FFFF0_0:
        CheckExternalEvents(cs1, 0xFFF0);
        // F000:FFF0 jmp short 0
        goto label_F000_0000_F0000_1;
    }

}
