using Spice86.Core.CLI;
using Spice86.Core.Emulator.CPU.Exceptions;
using Spice86.Core.Emulator.Function;
using Spice86.Core.Emulator.ReverseEngineer;
using Spice86.Core.Emulator.VM;
using Microsoft.Extensions.Logging;
using Spice86.Shared.Emulator.Memory;
using System;
using System.Collections.Generic;

namespace Spice86.Generated;

public sealed class CfgGeneratedOverrideSupplier : IOverrideSupplier {
    public IDictionary<SegmentedAddress, FunctionInformation> GenerateFunctionInformations(ILogger loggerService, Configuration configuration, ushort programStartAddress, Machine machine) {
        return new CfgGeneratedOverrides(new Dictionary<SegmentedAddress, FunctionInformation>(), machine, loggerService, configuration, programStartAddress).FunctionInformations;
    }
}

public class CfgGeneratedOverrides : CSharpOverrideHelper {
    protected readonly ushort cs1;

    public CfgGeneratedOverrides(IDictionary<SegmentedAddress, FunctionInformation> functionInformations, Machine machine, ILogger loggerService, Configuration configuration, ushort programStartSegment) : base(functionInformations, machine, loggerService, configuration) {
        cs1 = 0xF000;

        DefineFunction(cs1, 0xFFF0, unknown_F000_FFF0_FFFF0);
    }

    public virtual Action unknown_F000_FFF0_FFFF0(int loadOffset) {
        goto label_F000_FFF0_FFFF0_0;

    label_F000_0000_F0000_1:
        CheckExternalEvents(cs1, 0x0000);
        // F000:0000 mov AX,0x3B5E
        AX = (ushort)0x3B5E;
        // F000:0003 mov BX,0xC8A7
        BX = (ushort)0xC8A7;
        // F000:0006 mov word ptr DS:[0],0x2072
        UInt16[DS, (ushort)0x0000] = (ushort)0x2072;
        // F000:000C mov word ptr DS:[2],0x3E79
        UInt16[DS, (ushort)0x0002] = (ushort)0x3E79;
        // F000:0012 mov SP,0x00A0
        SP = (ushort)0x00A0;
        // F000:0015 rcl AX,1
        AX = Alu16.Rcl(AX, unchecked((byte)1));
        // F000:0017 pushf
        Stack.Push16(FlagRegister16);
        // F000:0018 mov word ptr DS:[0x0020],AX
        UInt16[DS, (ushort)0x0020] = AX;
        // F000:001B rcl word ptr DS:[0],1
        UInt16[DS, (ushort)0x0000] = Alu16.Rcl(UInt16[DS, (ushort)0x0000], unchecked((byte)1));
        // F000:001F pushf
        Stack.Push16(FlagRegister16);
        // F000:0020 mov CX,0x0100
        CX = (ushort)0x0100;
        // F000:0023 rcl BX,CL
        BX = Alu16.Rcl(BX, CL);
        // F000:0025 pushf
        Stack.Push16(FlagRegister16);
        // F000:0026 mov word ptr DS:[0x0022],BX
        UInt16[DS, (ushort)0x0022] = BX;
        // F000:002A mov CX,0xFFFF
        CX = (ushort)0xFFFF;
        // F000:002D mov DX,BX
        DX = BX;
        // F000:002F rcl DX,CL
        DX = Alu16.Rcl(DX, CL);
        // F000:0031 pushf
        Stack.Push16(FlagRegister16);
        // F000:0032 mov word ptr DS:[0x0024],DX
        UInt16[DS, (ushort)0x0024] = DX;
        // F000:0036 mov CL,8
        CL = (byte)0x08;
        // F000:0038 rcl BX,CL
        BX = Alu16.Rcl(BX, CL);
        // F000:003A pushf
        Stack.Push16(FlagRegister16);
        // F000:003B mov word ptr DS:[0x0026],BX
        UInt16[DS, (ushort)0x0026] = BX;
        // F000:003F mov CL,4
        CL = (byte)0x04;
        // F000:0041 rcl word ptr DS:[2],CL
        UInt16[DS, (ushort)0x0002] = Alu16.Rcl(UInt16[DS, (ushort)0x0002], CL);
        // F000:0045 pushf
        Stack.Push16(FlagRegister16);
        // F000:0046 mov DX,0x5904
        DX = (ushort)0x5904;
        // F000:0049 mov AX,0xBE7C
        AX = (ushort)0xBE7C;
        // F000:004C mov word ptr DS:[4],0xD62F
        UInt16[DS, (ushort)0x0004] = (ushort)0xD62F;
        // F000:0052 mov word ptr DS:[6],0x6FD8
        UInt16[DS, (ushort)0x0006] = (ushort)0x6FD8;
        // F000:0058 rcl AH,1
        AH = Alu8.Rcl(AH, unchecked((byte)1));
        // F000:005A pushf
        Stack.Push16(FlagRegister16);
        // F000:005B mov word ptr DS:[0x0028],AX
        UInt16[DS, (ushort)0x0028] = AX;
        // F000:005E rcl byte ptr DS:[5],1
        UInt8[DS, (ushort)0x0005] = Alu8.Rcl(UInt8[DS, (ushort)0x0005], unchecked((byte)1));
        // F000:0062 pushf
        Stack.Push16(FlagRegister16);
        // F000:0063 mov CL,7
        CL = (byte)0x07;
        // F000:0065 rcl DL,CL
        DL = Alu8.Rcl(DL, CL);
        // F000:0067 pushf
        Stack.Push16(FlagRegister16);
        // F000:0068 mov word ptr DS:[0x002A],DX
        UInt16[DS, (ushort)0x002A] = DX;
        // F000:006C rcl byte ptr DS:[6],CL
        UInt8[DS, (ushort)0x0006] = Alu8.Rcl(UInt8[DS, (ushort)0x0006], CL);
        // F000:0070 pushf
        Stack.Push16(FlagRegister16);
        // F000:0071 mov AX,0x15D6
        AX = (ushort)0x15D6;
        // F000:0074 mov BX,0x8307
        BX = (ushort)0x8307;
        // F000:0077 mov word ptr DS:[8],0x9AB7
        UInt16[DS, (ushort)0x0008] = (ushort)0x9AB7;
        // F000:007D mov word ptr DS:[0x000A],0x28B6
        UInt16[DS, (ushort)0x000A] = (ushort)0x28B6;
        // F000:0083 rcr AX,1
        AX = Alu16.Rcr(AX, 1);
        // F000:0085 pushf
        Stack.Push16(FlagRegister16);
        // F000:0086 mov word ptr DS:[0x002C],AX
        UInt16[DS, (ushort)0x002C] = AX;
        // F000:0089 rcr word ptr DS:[8],1
        UInt16[DS, (ushort)0x0008] = Alu16.Rcr(UInt16[DS, (ushort)0x0008], 1);
        // F000:008D pushf
        Stack.Push16(FlagRegister16);
        // F000:008E mov CX,0x0100
        CX = (ushort)0x0100;
        // F000:0091 rcr BX,CL
        BX = Alu16.Rcr(BX, unchecked((int)CL));
        // F000:0093 pushf
        Stack.Push16(FlagRegister16);
        // F000:0094 mov word ptr DS:[0x002E],BX
        UInt16[DS, (ushort)0x002E] = BX;
        // F000:0098 mov CX,0xFFFF
        CX = (ushort)0xFFFF;
        // F000:009B mov DX,BX
        DX = BX;
        // F000:009D rcr DX,CL
        DX = Alu16.Rcr(DX, unchecked((int)CL));
        // F000:009F pushf
        Stack.Push16(FlagRegister16);
        // F000:00A0 mov word ptr DS:[0x0030],DX
        UInt16[DS, (ushort)0x0030] = DX;
        // F000:00A4 mov CL,5
        CL = (byte)0x05;
        // F000:00A6 rcr BX,CL
        BX = Alu16.Rcr(BX, unchecked((int)CL));
        // F000:00A8 pushf
        Stack.Push16(FlagRegister16);
        // F000:00A9 mov word ptr DS:[0x0032],BX
        UInt16[DS, (ushort)0x0032] = BX;
        // F000:00AD mov CL,4
        CL = (byte)0x04;
        // F000:00AF rcr word ptr DS:[0x000A],CL
        UInt16[DS, (ushort)0x000A] = Alu16.Rcr(UInt16[DS, (ushort)0x000A], unchecked((int)CL));
        // F000:00B3 pushf
        Stack.Push16(FlagRegister16);
        // F000:00B4 mov DX,0x7EAA
        DX = (ushort)0x7EAA;
        // F000:00B7 mov AX,0x3A8D
        AX = (ushort)0x3A8D;
        // F000:00BA mov word ptr DS:[0x000C],0xA414
        UInt16[DS, (ushort)0x000C] = (ushort)0xA414;
        // F000:00C0 mov word ptr DS:[0x000E],0x2838
        UInt16[DS, (ushort)0x000E] = (ushort)0x2838;
        // F000:00C6 rcr AH,1
        AH = Alu8.Rcr(AH, 1);
        // F000:00C8 pushf
        Stack.Push16(FlagRegister16);
        // F000:00C9 mov word ptr DS:[0x0034],AX
        UInt16[DS, (ushort)0x0034] = AX;
        // F000:00CC rcr byte ptr DS:[0x000D],1
        UInt8[DS, (ushort)0x000D] = Alu8.Rcr(UInt8[DS, (ushort)0x000D], 1);
        // F000:00D0 pushf
        Stack.Push16(FlagRegister16);
        // F000:00D1 mov CL,7
        CL = (byte)0x07;
        // F000:00D3 rcr DL,CL
        DL = Alu8.Rcr(DL, unchecked((int)CL));
        // F000:00D5 pushf
        Stack.Push16(FlagRegister16);
        // F000:00D6 mov word ptr DS:[0x0036],DX
        UInt16[DS, (ushort)0x0036] = DX;
        // F000:00DA rcr byte ptr DS:[0x000E],CL
        UInt8[DS, (ushort)0x000E] = Alu8.Rcr(UInt8[DS, (ushort)0x000E], unchecked((int)CL));
        // F000:00DE pushf
        Stack.Push16(FlagRegister16);
        // F000:00DF mov AX,0x020D
        AX = (ushort)0x020D;
        // F000:00E2 mov BX,0x8D5A
        BX = (ushort)0x8D5A;
        // F000:00E5 mov word ptr DS:[0x0010],0x28DD
        UInt16[DS, (ushort)0x0010] = (ushort)0x28DD;
        // F000:00EB mov word ptr DS:[0x0012],0xD74A
        UInt16[DS, (ushort)0x0012] = (ushort)0xD74A;
        // F000:00F1 rol AX,1
        AX = Alu16.Rol(AX, unchecked((byte)1));
        // F000:00F3 pushf
        Stack.Push16(FlagRegister16);
        // F000:00F4 mov word ptr DS:[0x0038],AX
        UInt16[DS, (ushort)0x0038] = AX;
        // F000:00F7 rol word ptr DS:[0x0010],1
        UInt16[DS, (ushort)0x0010] = Alu16.Rol(UInt16[DS, (ushort)0x0010], unchecked((byte)1));
        // F000:00FB pushf
        Stack.Push16(FlagRegister16);
        // F000:00FC mov CX,0x0100
        CX = (ushort)0x0100;
        // F000:00FF rol BX,CL
        BX = Alu16.Rol(BX, CL);
        // F000:0101 pushf
        Stack.Push16(FlagRegister16);
        // F000:0102 mov word ptr DS:[0x003A],BX
        UInt16[DS, (ushort)0x003A] = BX;
        // F000:0106 mov CX,0xFFFF
        CX = (ushort)0xFFFF;
        // F000:0109 mov DX,BX
        DX = BX;
        // F000:010B rol DX,CL
        DX = Alu16.Rol(DX, CL);
        // F000:010D pushf
        Stack.Push16(FlagRegister16);
        // F000:010E mov word ptr DS:[0x003C],DX
        UInt16[DS, (ushort)0x003C] = DX;
        // F000:0112 mov CL,4
        CL = (byte)0x04;
        // F000:0114 rol BX,CL
        BX = Alu16.Rol(BX, CL);
        // F000:0116 pushf
        Stack.Push16(FlagRegister16);
        // F000:0117 mov word ptr DS:[0x003E],BX
        UInt16[DS, (ushort)0x003E] = BX;
        // F000:011B mov CL,4
        CL = (byte)0x04;
        // F000:011D rol word ptr DS:[0x0012],CL
        UInt16[DS, (ushort)0x0012] = Alu16.Rol(UInt16[DS, (ushort)0x0012], CL);
        // F000:0121 pushf
        Stack.Push16(FlagRegister16);
        // F000:0122 mov DX,0x9D09
        DX = (ushort)0x9D09;
        // F000:0125 mov AX,0xC948
        AX = (ushort)0xC948;
        // F000:0128 mov word ptr DS:[0x0014],0x0B80
        UInt16[DS, (ushort)0x0014] = (ushort)0x0B80;
        // F000:012E mov word ptr DS:[0x0016],0x48E8
        UInt16[DS, (ushort)0x0016] = (ushort)0x48E8;
        // F000:0134 rol AH,1
        AH = Alu8.Rol(AH, unchecked((byte)1));
        // F000:0136 pushf
        Stack.Push16(FlagRegister16);
        // F000:0137 mov word ptr DS:[0x0040],AX
        UInt16[DS, (ushort)0x0040] = AX;
        // F000:013A rol byte ptr DS:[0x0015],1
        UInt8[DS, (ushort)0x0015] = Alu8.Rol(UInt8[DS, (ushort)0x0015], unchecked((byte)1));
        // F000:013E pushf
        Stack.Push16(FlagRegister16);
        // F000:013F mov CL,7
        CL = (byte)0x07;
        // F000:0141 rol DL,CL
        DL = Alu8.Rol(DL, CL);
        // F000:0143 pushf
        Stack.Push16(FlagRegister16);
        // F000:0144 mov word ptr DS:[0x0042],DX
        UInt16[DS, (ushort)0x0042] = DX;
        // F000:0148 rol byte ptr DS:[0x0016],CL
        UInt8[DS, (ushort)0x0016] = Alu8.Rol(UInt8[DS, (ushort)0x0016], CL);
        // F000:014C pushf
        Stack.Push16(FlagRegister16);
        // F000:014D mov AX,0xF25E
        AX = (ushort)0xF25E;
        // F000:0150 mov BX,0x2EB5
        BX = (ushort)0x2EB5;
        // F000:0153 mov word ptr DS:[0x0018],0x0151
        UInt16[DS, (ushort)0x0018] = (ushort)0x0151;
        // F000:0159 mov word ptr DS:[0x001A],0x7237
        UInt16[DS, (ushort)0x001A] = (ushort)0x7237;
        // F000:015F ror AX,1
        AX = Alu16.Ror(AX, 1);
        // F000:0161 pushf
        Stack.Push16(FlagRegister16);
        // F000:0162 mov word ptr DS:[0x0044],AX
        UInt16[DS, (ushort)0x0044] = AX;
        // F000:0165 ror word ptr DS:[0x0018],1
        UInt16[DS, (ushort)0x0018] = Alu16.Ror(UInt16[DS, (ushort)0x0018], 1);
        // F000:0169 pushf
        Stack.Push16(FlagRegister16);
        // F000:016A mov CX,0x0100
        CX = (ushort)0x0100;
        // F000:016D ror BX,CL
        BX = Alu16.Ror(BX, unchecked((int)CL));
        // F000:016F pushf
        Stack.Push16(FlagRegister16);
        // F000:0170 mov word ptr DS:[0x0046],BX
        UInt16[DS, (ushort)0x0046] = BX;
        // F000:0174 mov CX,0xFFFF
        CX = (ushort)0xFFFF;
        // F000:0177 mov DX,BX
        DX = BX;
        // F000:0179 ror DX,CL
        DX = Alu16.Ror(DX, unchecked((int)CL));
        // F000:017B pushf
        Stack.Push16(FlagRegister16);
        // F000:017C mov word ptr DS:[0x0048],DX
        UInt16[DS, (ushort)0x0048] = DX;
        // F000:0180 mov CL,4
        CL = (byte)0x04;
        // F000:0182 ror BX,CL
        BX = Alu16.Ror(BX, unchecked((int)CL));
        // F000:0184 pushf
        Stack.Push16(FlagRegister16);
        // F000:0185 mov word ptr DS:[0x004A],BX
        UInt16[DS, (ushort)0x004A] = BX;
        // F000:0189 mov CL,4
        CL = (byte)0x04;
        // F000:018B ror word ptr DS:[0x001A],CL
        UInt16[DS, (ushort)0x001A] = Alu16.Ror(UInt16[DS, (ushort)0x001A], unchecked((int)CL));
        // F000:018F pushf
        Stack.Push16(FlagRegister16);
        // F000:0190 mov DX,0x4288
        DX = (ushort)0x4288;
        // F000:0193 mov AX,0x8BAB
        AX = (ushort)0x8BAB;
        // F000:0196 mov word ptr DS:[0x001C],0x5DD9
        UInt16[DS, (ushort)0x001C] = (ushort)0x5DD9;
        // F000:019C mov word ptr DS:[0x001E],0xC7F7
        UInt16[DS, (ushort)0x001E] = (ushort)0xC7F7;
        // F000:01A2 ror AH,1
        AH = Alu8.Ror(AH, 1);
        // F000:01A4 pushf
        Stack.Push16(FlagRegister16);
        // F000:01A5 mov word ptr DS:[0x004C],AX
        UInt16[DS, (ushort)0x004C] = AX;
        // F000:01A8 ror byte ptr DS:[0x001D],1
        UInt8[DS, (ushort)0x001D] = Alu8.Ror(UInt8[DS, (ushort)0x001D], 1);
        // F000:01AC pushf
        Stack.Push16(FlagRegister16);
        // F000:01AD mov CL,7
        CL = (byte)0x07;
        // F000:01AF ror DL,CL
        DL = Alu8.Ror(DL, unchecked((int)CL));
        // F000:01B1 pushf
        Stack.Push16(FlagRegister16);
        // F000:01B2 mov word ptr DS:[0x004E],DX
        UInt16[DS, (ushort)0x004E] = DX;
        // F000:01B6 ror byte ptr DS:[0x001E],CL
        UInt8[DS, (ushort)0x001E] = Alu8.Ror(UInt8[DS, (ushort)0x001E], unchecked((int)CL));
        // F000:01BA pushf
        Stack.Push16(FlagRegister16);
        // F000:01BB hlt
        return Hlt();
    label_F000_FFF0_FFFF0_0:
        CheckExternalEvents(cs1, 0xFFF0);
        // F000:FFF0 jmp short 0
        goto label_F000_0000_F0000_1;
    }

}
