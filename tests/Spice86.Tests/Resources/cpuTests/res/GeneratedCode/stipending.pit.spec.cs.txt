using Spice86.Core.CLI;
using Spice86.Core.Emulator.CPU.Exceptions;
using Spice86.Core.Emulator.Function;
using Spice86.Core.Emulator.ReverseEngineer;
using Spice86.Core.Emulator.VM;
using Microsoft.Extensions.Logging;
using Spice86.Shared.Emulator.Memory;
using System;
using System.Collections.Generic;

namespace Spice86.Generated;

public sealed class CfgGeneratedOverrideSupplier : IOverrideSupplier {
    public IDictionary<SegmentedAddress, FunctionInformation> GenerateFunctionInformations(ILogger loggerService, Configuration configuration, ushort programStartAddress, Machine machine) {
        return new CfgGeneratedOverrides(new Dictionary<SegmentedAddress, FunctionInformation>(), machine, loggerService, configuration, programStartAddress).FunctionInformations;
    }
}

public class CfgGeneratedOverrides : CSharpOverrideHelper {
    protected readonly ushort cs1;

    public CfgGeneratedOverrides(IDictionary<SegmentedAddress, FunctionInformation> functionInformations, Machine machine, ILogger loggerService, Configuration configuration, ushort programStartSegment) : base(functionInformations, machine, loggerService, configuration) {
        cs1 = 0xF000;

        DefineFunction(cs1, 0x004E, unknown_F000_004E_F004E);
        DefineFunction(cs1, 0xFFF0, unknown_F000_FFF0_FFFF0);
    }

    public virtual Action unknown_F000_004E_F004E(int loadOffset) {
        CheckExternalEvents(cs1, 0x004E);
        // F000:004E push AX
        Stack.Push16(AX);
        // F000:004F mov DX,1
        DX = (ushort)0x0001;
        // F000:0052 mov AL,0x20
        AL = (byte)0x20;
        // F000:0054 out 0x20,AL
        Machine.IoPortDispatcher.WriteByte(unchecked((ushort)(byte)0x20), AL);
        // F000:0056 pop AX
        AX = Stack.Pop16();
        // F000:0057 iret
        return InterruptRet();
    }

    public virtual Action unknown_F000_FFF0_FFFF0(int loadOffset) {
        goto label_F000_FFF0_FFFF0_0;

    label_F000_0000_F0000_1:
        CheckExternalEvents(cs1, 0x0000);
        // F000:0000 mov AX,0
        AX = (ushort)0x0000;
        // F000:0003 mov SS,AX
        SS = AX;
        State.InterruptShadowing = true;
        // F000:0005 mov SP,0x0100
        SP = (ushort)0x0100;
        // F000:0008 mov DS,AX
        DS = AX;
        // F000:000A mov DX,0
        DX = (ushort)0x0000;
        // F000:000D mov word ptr DS:[0x0020],0x004E
        UInt16[DS, (ushort)0x0020] = (ushort)0x004E;
        // F000:0013 mov word ptr DS:[0x0022],CS
        UInt16[DS, (ushort)0x0022] = CS;
        // F000:0017 mov AL,0x36
        AL = (byte)0x36;
        // F000:0019 out 0x43,AL
        Machine.IoPortDispatcher.WriteByte(unchecked((ushort)(byte)0x43), AL);
        // F000:001B mov AL,0x51
        AL = (byte)0x51;
        // F000:001D out 0x40,AL
        Machine.IoPortDispatcher.WriteByte(unchecked((ushort)(byte)0x40), AL);
        // F000:001F mov AL,0x22
        AL = (byte)0x22;
        // F000:0021 out 0x40,AL
        Machine.IoPortDispatcher.WriteByte(unchecked((ushort)(byte)0x40), AL);
        // F000:0023 mov AL,0
        AL = (byte)0x00;
        // F000:0025 out 0x21,AL
        Machine.IoPortDispatcher.WriteByte(unchecked((ushort)(byte)0x21), AL);
        CheckExternalEvents(cs1, 0x0027);
        // F000:0027 cli
        InterruptFlag = false;
        // F000:0028 mov DX,0
        DX = (ushort)0x0000;
        // F000:002B mov ECX,0x00FFFFFF
        ECX = 0x00FFFFFFu;
    label_F000_0031_F0031_22:
        CheckExternalEvents(cs1, 0x0031);
        // F000:0031 loop 0x0031
        CX = unchecked((ushort)(CX - (ushort)0x0001));
        if (CX != (ushort)0x0000) {
            goto label_F000_0031_F0031_22;
        }
        CheckExternalEvents(cs1, 0x0033);
        // F000:0033 sti
        if (!InterruptFlag) {
            State.InterruptShadowing = true;
        }
        else {
        }
        InterruptFlag = true;
        CheckExternalEvents(cs1, 0x0034);
        // F000:0034 nop
        // F000:0035 mov word ptr DS:[0],DX
        UInt16[DS, (ushort)0x0000] = DX;
        // F000:0039 sti
        if (!InterruptFlag) {
            State.InterruptShadowing = true;
        }
        else {
        }
        InterruptFlag = true;
        CheckExternalEvents(cs1, 0x003A);
        // F000:003A pushf
        Stack.Push16(FlagRegister16);
        CheckExternalEvents(cs1, 0x003B);
        // F000:003B cli
        InterruptFlag = false;
        // F000:003C mov DX,0
        DX = (ushort)0x0000;
        // F000:003F mov ECX,0x00FFFFFF
        ECX = 0x00FFFFFFu;
    label_F000_0045_F0045_71:
        CheckExternalEvents(cs1, 0x0045);
        // F000:0045 loop 0x0045
        CX = unchecked((ushort)(CX - (ushort)0x0001));
        if (CX != (ushort)0x0000) {
            goto label_F000_0045_F0045_71;
        }
        CheckExternalEvents(cs1, 0x0047);
        // F000:0047 popf
        FlagRegister16 = Stack.Pop16();
        CheckExternalEvents(cs1, 0x0048);
        // F000:0048 nop
        // F000:0049 mov word ptr DS:[1],DX
        UInt16[DS, (ushort)0x0001] = DX;
        // F000:004D hlt
        return Hlt();
    label_F000_FFF0_FFFF0_0:
        CheckExternalEvents(cs1, 0xFFF0);
        // F000:FFF0 jmp short 0
        goto label_F000_0000_F0000_1;
    }

}
