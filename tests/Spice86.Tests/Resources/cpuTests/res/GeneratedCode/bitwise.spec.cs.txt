using Spice86.Core.CLI;
using Spice86.Core.Emulator.CPU.Exceptions;
using Spice86.Core.Emulator.Function;
using Spice86.Core.Emulator.ReverseEngineer;
using Spice86.Core.Emulator.VM;
using Microsoft.Extensions.Logging;
using Spice86.Shared.Emulator.Memory;
using System;
using System.Collections.Generic;

namespace Spice86.Generated;

public sealed class CfgGeneratedOverrideSupplier : IOverrideSupplier {
    public IDictionary<SegmentedAddress, FunctionInformation> GenerateFunctionInformations(ILogger loggerService, Configuration configuration, ushort programStartAddress, Machine machine) {
        return new CfgGeneratedOverrides(new Dictionary<SegmentedAddress, FunctionInformation>(), machine, loggerService, configuration, programStartAddress).FunctionInformations;
    }
}

public class CfgGeneratedOverrides : CSharpOverrideHelper {
    protected readonly ushort cs1;

    public CfgGeneratedOverrides(IDictionary<SegmentedAddress, FunctionInformation> functionInformations, Machine machine, ILogger loggerService, Configuration configuration, ushort programStartSegment) : base(functionInformations, machine, loggerService, configuration) {
        cs1 = 0xF000;

        DefineFunction(cs1, 0xFFF0, unknown_F000_FFF0_FFFF0);
    }

    public virtual Action unknown_F000_FFF0_FFFF0(int loadOffset) {
        goto label_F000_FFF0_FFFF0_0;

    label_F000_0000_F0000_1:
        CheckExternalEvents(cs1, 0x0000);
        // F000:0000 mov AX,0x7659
        AX = (ushort)0x7659;
        // F000:0003 mov BX,0x4BB8
        BX = (ushort)0x4BB8;
        // F000:0006 mov CX,0x3C84
        CX = (ushort)0x3C84;
        // F000:0009 mov word ptr DS:[0],0x1B76
        UInt16[DS, (ushort)0x0000] = (ushort)0x1B76;
        // F000:000F mov word ptr DS:[2],0x240B
        UInt16[DS, (ushort)0x0002] = (ushort)0x240B;
        // F000:0015 mov SP,0x0100
        SP = (ushort)0x0100;
        // F000:0018 and BX,AX
        BX = Alu16.And(BX, AX);
        // F000:001A pushf
        Stack.Push16(FlagRegister16);
        // F000:001B mov word ptr DS:[0x0020],BX
        UInt16[DS, (ushort)0x0020] = BX;
        // F000:001F and CX,word ptr DS:[2]
        CX = Alu16.And(CX, UInt16[DS, (ushort)0x0002]);
        // F000:0023 pushf
        Stack.Push16(FlagRegister16);
        // F000:0024 mov word ptr DS:[0x0022],CX
        UInt16[DS, (ushort)0x0022] = CX;
        // F000:0028 and word ptr DS:[0],CX
        UInt16[DS, (ushort)0x0000] = Alu16.And(UInt16[DS, (ushort)0x0000], CX);
        // F000:002C pushf
        Stack.Push16(FlagRegister16);
        // F000:002D and AX,0x4571
        AX = Alu16.And(AX, (ushort)0x4571);
        // F000:0030 pushf
        Stack.Push16(FlagRegister16);
        // F000:0031 mov word ptr DS:[0x0024],AX
        UInt16[DS, (ushort)0x0024] = AX;
        // F000:0034 and BX,0x27E9
        BX = Alu16.And(BX, (ushort)0x27E9);
        // F000:0038 pushf
        Stack.Push16(FlagRegister16);
        // F000:0039 mov word ptr DS:[0x0026],BX
        UInt16[DS, (ushort)0x0026] = BX;
        // F000:003D and word ptr DS:[2],0x3549
        UInt16[DS, (ushort)0x0002] = Alu16.And(UInt16[DS, (ushort)0x0002], (ushort)0x3549);
        // F000:0043 pushf
        Stack.Push16(FlagRegister16);
        // F000:0044 and AH,AL
        AH = Alu8.And(AH, AL);
        // F000:0046 pushf
        Stack.Push16(FlagRegister16);
        // F000:0047 mov byte ptr DS:[0x0028],AH
        UInt8[DS, (ushort)0x0028] = AH;
        // F000:004B and CL,byte ptr DS:[1]
        CL = Alu8.And(CL, UInt8[DS, (ushort)0x0001]);
        // F000:004F pushf
        Stack.Push16(FlagRegister16);
        // F000:0050 mov byte ptr DS:[0x0029],CL
        UInt8[DS, (ushort)0x0029] = CL;
        // F000:0054 and byte ptr DS:[3],CH
        UInt8[DS, (ushort)0x0003] = Alu8.And(UInt8[DS, (ushort)0x0003], CH);
        // F000:0058 pushf
        Stack.Push16(FlagRegister16);
        // F000:0059 and AL,0x46
        AL = Alu8.And(AL, (byte)0x46);
        // F000:005B pushf
        Stack.Push16(FlagRegister16);
        // F000:005C mov byte ptr DS:[0x002A],AL
        UInt8[DS, (ushort)0x002A] = AL;
        // F000:005F and BL,0x2D
        BL = Alu8.And(BL, (byte)0x2D);
        // F000:0062 pushf
        Stack.Push16(FlagRegister16);
        // F000:0063 mov byte ptr DS:[0x002B],BL
        UInt8[DS, (ushort)0x002B] = BL;
        // F000:0067 and byte ptr DS:[2],0xC6
        UInt8[DS, (ushort)0x0002] = Alu8.And(UInt8[DS, (ushort)0x0002], (byte)0xC6);
        // F000:006C pushf
        Stack.Push16(FlagRegister16);
        // F000:006D mov AX,0x05E3
        AX = (ushort)0x05E3;
        // F000:0070 mov BX,0xF877
        BX = (ushort)0xF877;
        // F000:0073 mov CX,0x4AE8
        CX = (ushort)0x4AE8;
        // F000:0076 mov DX,0x3B69
        DX = (ushort)0x3B69;
        // F000:0079 mov word ptr DS:[4],0x30C0
        UInt16[DS, (ushort)0x0004] = (ushort)0x30C0;
        // F000:007F mov word ptr DS:[6],0x5775
        UInt16[DS, (ushort)0x0006] = (ushort)0x5775;
        // F000:0085 mov word ptr DS:[8],0xFE66
        UInt16[DS, (ushort)0x0008] = (ushort)0xFE66;
        // F000:008B or BX,AX
        BX = Alu16.Or(BX, AX);
        // F000:008D pushf
        Stack.Push16(FlagRegister16);
        // F000:008E mov word ptr DS:[0x002C],BX
        UInt16[DS, (ushort)0x002C] = BX;
        // F000:0092 or CX,word ptr DS:[4]
        CX = Alu16.Or(CX, UInt16[DS, (ushort)0x0004]);
        // F000:0096 pushf
        Stack.Push16(FlagRegister16);
        // F000:0097 mov word ptr DS:[0x002E],CX
        UInt16[DS, (ushort)0x002E] = CX;
        // F000:009B or word ptr DS:[6],AX
        UInt16[DS, (ushort)0x0006] = Alu16.Or(UInt16[DS, (ushort)0x0006], AX);
        // F000:009F pushf
        Stack.Push16(FlagRegister16);
        // F000:00A0 or AX,0x41C3
        AX = Alu16.Or(AX, (ushort)0x41C3);
        // F000:00A3 pushf
        Stack.Push16(FlagRegister16);
        // F000:00A4 mov word ptr DS:[0x0030],AX
        UInt16[DS, (ushort)0x0030] = AX;
        // F000:00A7 or DX,0xB05D
        DX = Alu16.Or(DX, (ushort)0xB05D);
        // F000:00AB pushf
        Stack.Push16(FlagRegister16);
        // F000:00AC mov word ptr DS:[0x0032],DX
        UInt16[DS, (ushort)0x0032] = DX;
        // F000:00B0 or word ptr DS:[8],0x8D4C
        UInt16[DS, (ushort)0x0008] = Alu16.Or(UInt16[DS, (ushort)0x0008], (ushort)0x8D4C);
        // F000:00B6 pushf
        Stack.Push16(FlagRegister16);
        // F000:00B7 or AH,AL
        AH = Alu8.Or(AH, AL);
        // F000:00B9 pushf
        Stack.Push16(FlagRegister16);
        // F000:00BA mov byte ptr DS:[0x0034],AH
        UInt8[DS, (ushort)0x0034] = AH;
        // F000:00BE or CL,byte ptr DS:[5]
        CL = Alu8.Or(CL, UInt8[DS, (ushort)0x0005]);
        // F000:00C2 pushf
        Stack.Push16(FlagRegister16);
        // F000:00C3 mov byte ptr DS:[0x0035],CL
        UInt8[DS, (ushort)0x0035] = CL;
        // F000:00C7 or byte ptr DS:[6],CH
        UInt8[DS, (ushort)0x0006] = Alu8.Or(UInt8[DS, (ushort)0x0006], CH);
        // F000:00CB pushf
        Stack.Push16(FlagRegister16);
        // F000:00CC or AL,0x43
        AL = Alu8.Or(AL, (byte)0x43);
        // F000:00CE pushf
        Stack.Push16(FlagRegister16);
        // F000:00CF mov byte ptr DS:[0x0036],AL
        UInt8[DS, (ushort)0x0036] = AL;
        // F000:00D2 or BL,0x57
        BL = Alu8.Or(BL, (byte)0x57);
        // F000:00D5 pushf
        Stack.Push16(FlagRegister16);
        // F000:00D6 mov byte ptr DS:[0x0037],BL
        UInt8[DS, (ushort)0x0037] = BL;
        // F000:00DA or byte ptr DS:[7],0x54
        UInt8[DS, (ushort)0x0007] = Alu8.Or(UInt8[DS, (ushort)0x0007], (byte)0x54);
        // F000:00DF pushf
        Stack.Push16(FlagRegister16);
        // F000:00E0 mov AX,0xD0B4
        AX = (ushort)0xD0B4;
        // F000:00E3 mov BX,0x1BB8
        BX = (ushort)0x1BB8;
        // F000:00E6 mov CX,0x2B03
        CX = (ushort)0x2B03;
        // F000:00E9 mov DX,0xC3E6
        DX = (ushort)0xC3E6;
        // F000:00EC mov word ptr DS:[0x000A],0x3939
        UInt16[DS, (ushort)0x000A] = (ushort)0x3939;
        // F000:00F2 mov word ptr DS:[0x000C],0x864B
        UInt16[DS, (ushort)0x000C] = (ushort)0x864B;
        // F000:00F8 mov word ptr DS:[0x000E],0x8587
        UInt16[DS, (ushort)0x000E] = (ushort)0x8587;
        // F000:00FE xor BX,AX
        BX = Alu16.Xor(BX, AX);
        // F000:0100 pushf
        Stack.Push16(FlagRegister16);
        // F000:0101 mov word ptr DS:[0x0038],BX
        UInt16[DS, (ushort)0x0038] = BX;
        // F000:0105 xor CX,word ptr DS:[0x000A]
        CX = Alu16.Xor(CX, UInt16[DS, (ushort)0x000A]);
        // F000:0109 pushf
        Stack.Push16(FlagRegister16);
        // F000:010A mov word ptr DS:[0x003A],CX
        UInt16[DS, (ushort)0x003A] = CX;
        // F000:010E xor word ptr DS:[0x000C],AX
        UInt16[DS, (ushort)0x000C] = Alu16.Xor(UInt16[DS, (ushort)0x000C], AX);
        // F000:0112 pushf
        Stack.Push16(FlagRegister16);
        // F000:0113 xor AX,0x3D03
        AX = Alu16.Xor(AX, (ushort)0x3D03);
        // F000:0116 pushf
        Stack.Push16(FlagRegister16);
        // F000:0117 mov word ptr DS:[0x003C],AX
        UInt16[DS, (ushort)0x003C] = AX;
        // F000:011A xor DX,0x632D
        DX = Alu16.Xor(DX, (ushort)0x632D);
        // F000:011E pushf
        Stack.Push16(FlagRegister16);
        // F000:011F mov word ptr DS:[0x003E],DX
        UInt16[DS, (ushort)0x003E] = DX;
        // F000:0123 xor word ptr DS:[0x000E],0xCF07
        UInt16[DS, (ushort)0x000E] = Alu16.Xor(UInt16[DS, (ushort)0x000E], (ushort)0xCF07);
        // F000:0129 pushf
        Stack.Push16(FlagRegister16);
        // F000:012A xor AH,AL
        AH = Alu8.Xor(AH, AL);
        // F000:012C pushf
        Stack.Push16(FlagRegister16);
        // F000:012D mov byte ptr DS:[0x0040],AH
        UInt8[DS, (ushort)0x0040] = AH;
        // F000:0131 xor CL,byte ptr DS:[0x000B]
        CL = Alu8.Xor(CL, UInt8[DS, (ushort)0x000B]);
        // F000:0135 pushf
        Stack.Push16(FlagRegister16);
        // F000:0136 mov byte ptr DS:[0x0041],CL
        UInt8[DS, (ushort)0x0041] = CL;
        // F000:013A xor byte ptr DS:[0x000C],CH
        UInt8[DS, (ushort)0x000C] = Alu8.Xor(UInt8[DS, (ushort)0x000C], CH);
        // F000:013E pushf
        Stack.Push16(FlagRegister16);
        // F000:013F xor AL,0xB6
        AL = Alu8.Xor(AL, (byte)0xB6);
        // F000:0141 pushf
        Stack.Push16(FlagRegister16);
        // F000:0142 mov byte ptr DS:[0x0042],AL
        UInt8[DS, (ushort)0x0042] = AL;
        // F000:0145 xor BL,0xAE
        BL = Alu8.Xor(BL, (byte)0xAE);
        // F000:0148 pushf
        Stack.Push16(FlagRegister16);
        // F000:0149 mov byte ptr DS:[0x0043],BL
        UInt8[DS, (ushort)0x0043] = BL;
        // F000:014D xor byte ptr DS:[0x000D],0xDF
        UInt8[DS, (ushort)0x000D] = Alu8.Xor(UInt8[DS, (ushort)0x000D], (byte)0xDF);
        // F000:0152 pushf
        Stack.Push16(FlagRegister16);
        // F000:0153 mov AX,0x4D37
        AX = (ushort)0x4D37;
        // F000:0156 mov BX,0xDBE1
        BX = (ushort)0xDBE1;
        // F000:0159 mov CX,0x6549
        CX = (ushort)0x6549;
        // F000:015C mov DX,0x5CC4
        DX = (ushort)0x5CC4;
        // F000:015F mov word ptr DS:[0x0010],0xA8A8
        UInt16[DS, (ushort)0x0010] = (ushort)0xA8A8;
        // F000:0165 mov word ptr DS:[0x0012],0x35F6
        UInt16[DS, (ushort)0x0012] = (ushort)0x35F6;
        // F000:016B mov word ptr DS:[0x0014],0x4F00
        UInt16[DS, (ushort)0x0014] = (ushort)0x4F00;
        // F000:0171 and BX,AX
        Alu16.And(BX, AX);
        // F000:0173 pushf
        Stack.Push16(FlagRegister16);
        // F000:0174 mov word ptr DS:[0x0044],BX
        UInt16[DS, (ushort)0x0044] = BX;
        // F000:0178 and word ptr DS:[0x0010],CX
        Alu16.And(UInt16[DS, (ushort)0x0010], CX);
        // F000:017C pushf
        Stack.Push16(FlagRegister16);
        // F000:017D mov word ptr DS:[0x0046],CX
        UInt16[DS, (ushort)0x0046] = CX;
        // F000:0181 and word ptr DS:[0x0012],AX
        Alu16.And(UInt16[DS, (ushort)0x0012], AX);
        // F000:0185 pushf
        Stack.Push16(FlagRegister16);
        // F000:0186 test AX,0xDC6F
        Alu16.And(AX, (ushort)0xDC6F);
        // F000:0189 pushf
        Stack.Push16(FlagRegister16);
        // F000:018A mov word ptr DS:[0x0048],AX
        UInt16[DS, (ushort)0x0048] = AX;
        // F000:018D test DX,0x3046
        Alu16.And(DX, (ushort)0x3046);
        // F000:0191 pushf
        Stack.Push16(FlagRegister16);
        // F000:0192 mov word ptr DS:[0x004A],DX
        UInt16[DS, (ushort)0x004A] = DX;
        // F000:0196 test word ptr DS:[0x0014],0x96E4
        Alu16.And(UInt16[DS, (ushort)0x0014], (ushort)0x96E4);
        // F000:019C pushf
        Stack.Push16(FlagRegister16);
        // F000:019D and AH,AL
        Alu8.And(AH, AL);
        // F000:019F pushf
        Stack.Push16(FlagRegister16);
        // F000:01A0 mov byte ptr DS:[0x004C],AH
        UInt8[DS, (ushort)0x004C] = AH;
        // F000:01A4 and byte ptr DS:[0x000F],CL
        Alu8.And(UInt8[DS, (ushort)0x000F], CL);
        // F000:01A8 pushf
        Stack.Push16(FlagRegister16);
        // F000:01A9 mov byte ptr DS:[0x004D],CL
        UInt8[DS, (ushort)0x004D] = CL;
        // F000:01AD and byte ptr DS:[0x0010],CH
        Alu8.And(UInt8[DS, (ushort)0x0010], CH);
        // F000:01B1 pushf
        Stack.Push16(FlagRegister16);
        // F000:01B2 test AL,0xC0
        Alu8.And(AL, (byte)0xC0);
        // F000:01B4 pushf
        Stack.Push16(FlagRegister16);
        // F000:01B5 mov byte ptr DS:[0x004E],AL
        UInt8[DS, (ushort)0x004E] = AL;
        // F000:01B8 test BL,0xE0
        Alu8.And(BL, (byte)0xE0);
        // F000:01BB pushf
        Stack.Push16(FlagRegister16);
        // F000:01BC mov byte ptr DS:[0x004F],BL
        UInt8[DS, (ushort)0x004F] = BL;
        // F000:01C0 test byte ptr DS:[0x0011],0xBB
        Alu8.And(UInt8[DS, (ushort)0x0011], (byte)0xBB);
        // F000:01C5 pushf
        Stack.Push16(FlagRegister16);
        // F000:01C6 mov DX,0xBFA5
        DX = (ushort)0xBFA5;
        // F000:01C9 mov word ptr DS:[0x0016],0x4BE6
        UInt16[DS, (ushort)0x0016] = (ushort)0x4BE6;
        // F000:01CF mov word ptr DS:[0x0018],0xE9D2
        UInt16[DS, (ushort)0x0018] = (ushort)0xE9D2;
        // F000:01D5 mov AX,0x12B1
        AX = (ushort)0x12B1;
        // F000:01D8 push AX
        Stack.Push16(AX);
        CheckExternalEvents(cs1, 0x01D9);
        // F000:01D9 popf
        FlagRegister16 = Stack.Pop16();
        CheckExternalEvents(cs1, 0x01DA);
        // F000:01DA not DX
        DX = unchecked((ushort)~DX);
        // F000:01DC pushf
        Stack.Push16(FlagRegister16);
        // F000:01DD mov word ptr DS:[0x0050],DX
        UInt16[DS, (ushort)0x0050] = DX;
        // F000:01E1 not word ptr DS:[0x0016]
        UInt16[DS, (ushort)0x0016] = unchecked((ushort)~UInt16[DS, (ushort)0x0016]);
        // F000:01E5 pushf
        Stack.Push16(FlagRegister16);
        // F000:01E6 not DL
        DL = unchecked((byte)~DL);
        // F000:01E8 pushf
        Stack.Push16(FlagRegister16);
        // F000:01E9 mov byte ptr DS:[0x0052],DL
        UInt8[DS, (ushort)0x0052] = DL;
        // F000:01ED not byte ptr DS:[0x0018]
        UInt8[DS, (ushort)0x0018] = unchecked((byte)~UInt8[DS, (ushort)0x0018]);
        // F000:01F1 pushf
        Stack.Push16(FlagRegister16);
        // F000:01F2 hlt
        return Hlt();
    label_F000_FFF0_FFFF0_0:
        CheckExternalEvents(cs1, 0xFFF0);
        // F000:FFF0 jmp short 0
        goto label_F000_0000_F0000_1;
    }

}
